# Agent notes (Guardian)

This repo keeps product and AI guardrails in **`.cursor/rules/`**. When editing GuardianHQ (especially SwiftUI):

- **Two macOS apps (Training / Mission):** ``README_FULL.md`` → **Two-app SwiftPM products** and **Training ↔ Mission cutover — manual smoke**; shared **`GuardianCore`** library (`import GuardianCore`; SwiftPM product **`GuardianHQ`** aliases it); Training-only resources in **`GuardianTrainingSimulationResources`**; `swift build --product GuardianTraining` / `GuardianMission`. **`GuardianAppProduct`** drives splash, window title, sidebar logo, and which ``AppSection`` rails appear. Training omits Missions / Mission Control / Live Drive **nav** but keeps ``MissionStore`` + ``MissionControlStore`` for MRE/fleet hooks.
- **Gazebo 3D training worlds:** ``README_FULL.md`` → **Gazebo training simulation**; ``GazeboService`` / ``GazeboLaunchRecipe``; tracker ``ToDo/TrainingGazeboSimulationToDo.md``. **Single app-wide headless sim (world swap):** ``ToDo/GazeboAppWideInstanceToDo.md``. **Procedural terrain (World Builder):** ``ToDo/GazeboTerrainToDo.md``. **Builder scene extensions (water, terrain gen, model groups):** ``ToDo/BuilderGazeboToDo.md``. Training + full HQ only (`GuardianAppProduct/includesGazeboSimulation`). Stage runtime with `make gazebo-runtime`.

- **Plugin-owned fleet catalogue rows:** ``Sources/GuardianHQ/Plugins/PLUGIN_FLEET_CONTRIBUTIONS.md`` (manifest claims, JSON bodies, bootstrap).
- **Mission Control floating reserve pool (run envelope):** ``README_FULL.md`` → **Floating reserve pool (Mission Control run)**; deferred automation in ``NEXTVERSION.md`` → **Floating reserve pool — deferred phases** (2026-05-12).
- **Mission Control reserve swap-in (live active ↔ reserve):** backlog for arm/mission/reposition/commit pipeline, fixed `.reserve` + floating pool, triggers, and class matching — ``MissionRosterReservesToDo.md``.
- **Mission Control operator quick commands (MC-R triage):** typed whitelist → MRE dispatch (catalogue / recipe), executor vs direct, gates — ``OperatorCommandsIssuerToDo.md``.
- **Mission run task / slot / vehicle + MRE dispatch evidence:** Task truth lives in ``MissionRunEnvironment`` (``deriveMissionTaskState``, ``assignmentsBoundToMissionTask`` for §3 rollups). Slot policy lanes are written only through ``applySlotLifecycleLaneMutation`` and the §3 push/pull + ``MissionRunCommandSubsystem`` dispatch-outcome paths — avoid parallel slot-state maps. Before mutating lanes from fleet recipes, ``MissionRunRecipeOutcomeCorrelation`` must match the issued command. ``README_FULL.md``: **Mission run state model** (including **Modeling locks (v1 shipped)**). Stretch lifecycle backlog: ``TaskRosterAssignmentStatesToDo.md`` §8 until that file is retired.
- **MC-R ↔ Live Drive handoff (Engage, assignment handoff, return — manual Continue mission):** ``README_FULL.md`` → **Live Drive control session** (freestyle & mission, SIM & live).
- **Mission Control Run settings (app defaults vs per-run envelope):** ``README_FULL.md`` → **SIM battery drain** → **Mission Run settings — defaults vs per-run envelope** (clone at ``createRun``, never read ``GeneralSettingsStore`` from MC for those prefs). **SIM home reset on complete:** same subsection **SIM home reset on Mission Control run complete (v1 — product locked)**.
- **Mission Run SIM clean up (park all SITLs + optional teleport on complete):** product lock in ``README_FULL.md`` → **Mission Run SIM clean up (product lock)**. Wave concurrency for SIM **kill** / mission clear / geofence clear / battery: env ``GUARDIAN_MISSION_RUN_SIM_CLEANUP_MAX_CONCURRENT`` (see ``MissionRunSimCleanupConcurrency``).
- **Mission task between-cycles (Return to Launch / Loiter / Park):** ``README_FULL.md`` → **MRE policy & between-cycles dispatch** and **MRE mission template persistence** (Fleet recipes / Layer 1 list); deferred **Swap In Replacement** action — ``NEXTVERSION.md`` (2026-05-13).
- **Mission geofences:** Authoring, planner merge, policy, map overlays, optional `geofencePolygonsJSON` on mission upload, standalone geofence atoms/recipes, geofence clear on RTL teardown and run-complete SIM cleanup, and **distinct MRE log templates** for geofence encode failures (`missionGeofencePolygonsEncodeFailed`) and geofence fleet ack failures (`missionRunGeofenceFleetAckFailed`, including composite mission-upload stacks that fail on the geofence child) — ``README_FULL.md`` mission-recipes bullet and **Mission Run SIM clean up**.
- **Live Leaflet map performance:** ``README_FULL.md`` → **Live Leaflet map — bridge coalescing**.
- **MC-R SwiftUI observation restructure (narrow invalidation — roster / task / squad / fleet per stream):** ``README_FULL.md`` → **MC-R live UI row contracts** → **MC-R observation restructure — archived reference (v1 complete)** (goals, non-goals, invalidation inventory, hub entry-point matrix, file touch map). **Locked fleet strategy (A):** ``FleetVehicleLiveChannel.swift`` + ``FleetLinkService`` ``mcrRosterLiveChannel`` retain/release + ``publishHubFleetTelemetryTickAndRefreshMcrChannels`` (coalesced hub tick + per-retained-id channel refresh); last release removes the id from the in-memory channel map so churned stream keys do not accumulate shells (same README → **Phase 10 — manual smoke & channel registry**). **Fleet strategy write-up:** same section → **Fleet per-vehicle observation strategy (locked)**. **Where it lands in code (v1 shipped surfaces):** roster strip — ``MissionRunAssignmentLiveProjection`` / ``MCRRosterTileView`` (``MCRLiveRosterSnapshotStore.swift``, ``MCRRosterTileView.swift``); MCS setup roster slot + floating reserve berth cards — ``MCSRosterSlotFleetKeyedWrap.swift`` + keyed wrappers in ``MissionRunDetailView``; task list — ``MissionControlLiveTasksListSurface.swift`` + ``MCRLiveTaskListSnapshotStore.swift`` (primary-path hub merge); task triage hero — ``MCRLiveTaskTriageProgressCard.swift``; task triage static strip — ``MissionControlLiveTaskTriageProjection.swift``; live mission log strip — ``MCRLiveMissionLogStripStore.swift`` (ingest from ``MissionControlLiveRunRoot``); live map structure vs marker digests — ``MissionRunDetailView`` (``liveOverviewMapStructureIdentity``, ``liveOverviewMapMarkerCoordinateDigest`` / fence authoring digest); vehicle + floating reserve berth overlays — ``MCRLiveVehicleOverlayBadgesCard.swift``; floating pool browse/swap tiles — ``MCRLiveReservePoolBerthProjection.swift`` + ``MissionControlLiveReservePoolStripViews.swift``. **Convention:** do not add ``@ObservedObject var run`` or ``@ObservedObject var fleetLink`` on new MC-R **row / tile / overlay badge** views when a resolved bridge ``vehicleID`` exists — observe ``FleetVehicleLiveChannel`` for that id and merge equatable snapshots from coordinators/stores. Shell chrome (``MissionRunDetailView``) may still observe ``run`` / ``fleetLink`` for routing and actions.
- **MRE squads conversion:** ``MRESquadsToDo.md``; ``README_FULL.md`` → **Mission task authoring (`MissionTask`)** (includes **Squad store / persistence** and **Wingman convoy follow (v1 — shipped)**). Squad state + execution: per-squad autostart, ``MissionRunCommandTarget/squad``, ``releaseNextDeferredFirstWaveSquad``. Observers: ``MissionRunObserverIntegration.swift`` (``MissionControlRunObserver`` receives ``MissionRunEnvironment``). Ordering/labels: ``MissionControlSquadUtilities.swift``. Wingman follow subsystem: ``MissionRunSquadFollowSubsystem``; checklist: ``ToDo/SquadFollow&Formation.md`` (v1 complete except SIM smoke); operator prompt: ``MissionRunSquadFollowOperatorPromptBridge``; RoE hooks: ``performSquadPromoteWingmanToPrimary``, ``performRosterReleaseWingmanFromSquadFollow``; MC-R wingman status via ``wingmanFollowPhase`` + ``squadFollowStatusRevision`` → ``MCRLiveRosterRowSnapshotFactory``.
- **macOS `.app` bundle + UserNotifications (TCC, UNUserNotificationCenter, operator prompt OOA):** ``ToDo/AppMacOSBundleUserNotificationsToDo.md`` (also linked from ``ToDo/TODO.md`` → **App System**).
- **Theme & tokens:** `.cursor/rules/guardian-theme-tokens.mdc` — use `GuardianTheme.palette`, `GuardianSemanticColors`, `GuardianSpacing`, `GuardianTypography`, and related design-system types when they exist instead of inventing parallel styling.
- **Shared utilities:** `.cursor/rules/global-utilities-placement.mdc` — reusable helpers for multiple systems/plugins go under `Sources/GuardianHQ/Systems/Utilities/` and the `Utilities` / `GlobalUtilities` namespace.
- **Buttons:** `.cursor/rules/button-semantic-colors.mdc`
- **Drawers:** `.cursor/rules/app-drawer.mdc`
- **Modals / confirms:** `.cursor/rules/modal-central-template.mdc`, `.cursor/rules/guardian-confirm-dialogs.mdc`
- **Build / version / missions / tests:** See the other rules in `.cursor/rules/` as relevant.
- **Operator copy:** `.cursor/rules/no-future-version-user-copy.mdc` — no “future build” / “coming soon” teases in UI; describe the current product only.
- **SITL random MAVLink port / sysid:** ``README_FULL.md`` → **Built-in SITL MAVLink endpoints**; wipe/orphan backlog — ``SITLRandomPortToDo.md``.
- **Stack wiki (PX4 / ArduPilot docs for agents):** ``Resources/StackWiki/`` + ``README_FULL.md`` → **Stack wiki (local autopilot docs)**; refresh ``make stack-wiki-refresh``; retrieval policy ``.cursor/rules/stack-wiki-docs-local.mdc``.
- **ROS 2 autonomy (Nav2 / Aerostack2, fleet-wide sidecar):** ``Sources/GuardianHQ/Resources/Ros2VehicleBridge/README_AUTONOMY.md`` — **ROS sidecar rollout** (Training first; Formation / MCR / live follow). Enroll streams with ``FleetLinkService/ensurePx4Ros2Sidecar(forVehicleID:)`` after MAVLink position is up; do not start the bridge at raw SITL register for every garage spawn. **Orphan cleanup:** ``GuardianRos2OrphanBlitz`` + ``GuardianRos2SpawnRegistry`` (cold launch, ``stopAll``, app quit; `GUARDIAN_SKIP_ROS2_ORPHAN_BLITZ=1`) — ``README_FULL.md`` → **Child processes and cold launch**.

**Theme reference:** use the in-app **Theme** plugin (`ThemePanelView` / `ThemeCatalogContent`) as the living catalog; rules live in `guardian-theme-tokens.mdc` above.
