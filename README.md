# Guardian

Guardian is now a native macOS app shell built with SwiftUI.

## Current app shell

- Dark-first HQ interface
- Left navigation: `Dashboard`, `Devices`, `Missions`, `Mission Control`
- Top bar with app title
- Per-page content title/subtitle so navigation changes are obvious

## Build and run

From the `Guardian` directory (SwiftPM):

```bash
swift build
swift run GuardianHQ
```

Or use the Makefile (also fetches bundled `mavsdk_server` on first build), then run the binary SwiftPM emitted (path varies by architecture):

```bash
make build
"$(swift build --show-bin-path)/GuardianHQ"
```

Open the package in Xcode if you prefer:

```bash
open Package.swift
```

Then **Product → Run** (⌘R). Xcode uses the same SwiftPM target.

### Optional one-time dependencies (SITL / bridge)

- **ArduPilot SITL bundle:** `make sitl-runtime` — Python for `sim_vehicle`: `make sitl-deps`
- **PX4 SITL runtime slice** (`bin/px4` + `etc/`): build PX4 elsewhere, then `PX4_AUTOPILOT_ROOT=/path/to/PX4-Autopilot make px4-sitl-runtime`
- **Prewarm both SITL stacks:** `make sitl-prewarm` (ArduPilot full prebuild), or include PX4 too: `PX4_AUTOPILOT_ROOT=/path/to/PX4-Autopilot make sitl-prewarm`
- **MAVSDK-Python bridge:** `make bridge-deps`

## SITL command-catalogue smoke tests

End-to-end coverage for the Layer 0 fleet command catalogue (`command.fleet.vehicle.*`) ships as an **opt-in** XCTest suite, `GuardianHQSitlSmokeTests` in `Tests/GuardianHQTests/`. It is not part of normal `swift test` runs because it boots real **ArduPilot** and **PX4** SITL instances, starts `mavsdk_server` sessions, and drives the real `SitlService` → `FleetLinkService` → `FleetCommandsCatalogue` path against them. The same harness also runs **Layer 1** recipes through `FleetRecipeRunner`: Pattern-A `recipe.fleet.calibrate.{compass,accelerometer,gyro,baro,level}`, two-step `recipe.fleet.diagnose.armprobe`, the composite `recipe.fleet.errors.fix.calibrationrequired`, and **cancel mid-recipe** (`test_cancelMidRecipe_runsDeclaredCancelRecipe_cancelCalibration_sitlBothStacks` — wraps the catalogue to assert `cancelRecipe` dispatches `command.fleet.vehicle.cancel.calibration`). **Recipe escalation → operator prompt** routing (lift, ``OperatorPromptRouter``, ``OperatorPromptResumptionChannel``) is covered in normal `swift test` by ``RecipeEscalationOperatorPromptIntegrationTests`` — see **Fleet Commands & Recipes architecture → Stage G**.

### Run the suite

The canonical entry point is the repo script:

```bash
scripts/run_sitl_smoke_tests.sh
```

The script sets `GUARDIAN_RUN_SITL_SMOKE=1` and filters `swift test` to the smoke suite. Without that env var, the tests `XCTSkip` themselves so `swift test` (CI, dev loop) stays fast and deterministic.

Prerequisites are the same as for in-app SITL: bundled ArduPilot tree (`make sitl-runtime` or developer checkout via `GUARDIAN_ARDUPILOT_ROOT`), PX4 SITL slice (`make px4-sitl-runtime` or `GUARDIAN_PX4_ROOT` pointing at a PX4-Autopilot checkout with `px4_sitl_default` already built), and the SITL Python deps (`make sitl-deps`).

### What it covers

In one shared session per stack, for both ArduPilot and PX4:

- **Telemetry reads** — every `get.telemetry.*` descriptor returns a populated `keyValues` payload.
- **Param-set calibration** — `do.calibrate.battery.capacity` round-trips through the catalogue's `PARAM_SET` + read-back path.
- **Set-mode** — `do.mode mode=hold` succeeds and the autopilot's reported flight mode flips to a hold/loiter token.
- **Arm / disarm** — `do.arm` succeeds once the vehicle reports `healthArmable`, telemetry transitions to armed, then `do.disarm` brings it back down.
- **Loiter / return-home / land** — `do.loiter`, `do.return.home`, and `do.land` are accepted by the autopilot after re-arm.
- **PX4 calibration plugin** (PX4 only) — `do.calibrate.{gyro, level, gimbal}` exercises the MAVSDK Calibration plugin transport and surfaces a non-transport outcome (success, declined, did-not-converge, busy, etc.).
- **Pattern-A calibration recipes** (both stacks) — `test_recipeFleetCalibrate{Compass,Accelerometer,Gyro,Baro,Level}_endToEnd_againstArduPilotAndPX4SITL` drive the matching `recipe.fleet.calibrate.*` bodies through `FleetRecipeRunner` → `do.calibrate.*`. Assertions (shared helper) allow success, procedure-class errors with escalation + default abort, or catalogue **timeout** on the step (per-step dispatch is capped at ``FleetCommandsCatalogue/defaultDispatchTimeoutSeconds``), and reject catalogue / wiring failures.
- **Arm probe** — `test_recipeFleetDiagnoseArmprobe_endToEnd_againstArduPilotAndPX4SITL` runs `recipe.fleet.diagnose.armprobe` (arm → disarm). Assertions accept success, explicit arm refusal kinds from the recipe body, timeout/cancel on arm, or any failure once the failing step is **disarm** (proves the two-step path executed).
- **Calibration-required composite** — `test_recipeFleetErrorsFixCalibrationRequired_endToEnd_againstArduPilotAndPX4SITL` runs `recipe.fleet.errors.fix.calibrationrequired` (nested compass → accelerometer → gyro → armprobe). A full green path is optional; failures must match one of the **authored parent** `fail` detail strings (child recipe or probe did not succeed), not wiring / registration errors.
- **Cancel + cleanup** — `test_cancelMidRecipe_runsDeclaredCancelRecipe_cancelCalibration_sitlBothStacks` starts `recipe.fleet.calibrate.compass`, schedules ``FleetRecipeRunner/cancel(vehicleID:)`` while the compass command is in flight, and asserts the catalogue saw ``command.fleet.vehicle.cancel.calibration`` (from ``recipe.fleet.calibrate.cancel`` on the descriptor).

### Tuning timeouts

The harness honours these environment variables when set; otherwise it uses the defaults shown:

| Variable | Default (s) | Used for |
| --- | --- | --- |
| `GUARDIAN_SITL_SMOKE_BOOT_TIMEOUT` | `180` | Waiting for each SITL session to reach `.live` and for `healthArmable`. |
| `GUARDIAN_SITL_SMOKE_COMMAND_TIMEOUT` | `45` | Per-command catalogue dispatch budget. |
| `GUARDIAN_SITL_SMOKE_SIDE_EFFECT_TIMEOUT` | `20` | Waiting for telemetry side-effects (armed state, mode change). |
| `GUARDIAN_SITL_SMOKE_CALIBRATION_TIMEOUT` | `90` | PX4 calibration plugin per-procedure budget. |

### Extending it

When you wire a new command in `FleetCommandsCatalogue`, add a matching smoke assertion to `GuardianHQSitlSmokeTests` (in the same shared-session loop) per the **Add Tests For New Features** rule. Use `invoke(...)` + `assertSuccess(...)` for happy-path commands, and `assertNoTransportFailure(...)` for procedures whose autopilot-side outcome is non-deterministic in SITL (e.g. calibration progress).

## SIM battery drain (SITL)

Guardian can **turn simulated pack depletion on or off** for built-in SITL vehicles from **LiveDrive** and **Mission Control → Running**, so operational flows see realistic battery movement instead of a static 100%. This is **stack-agnostic in the app** but implemented with **different autopilot parameters** and **different simulator rules** on the wire.

### Where to configure it

- **Settings → Live Drive → SIMs** — **None** / Slow / Normal / Fast on ``GeneralSettingsStore/liveDriveSimBatteryDrainRate`` (default normal). Used when **Live Drive** starts a **freestyle** SIM control session and from the Live Drive **SIM live settings** drawer. Mission Control **running** SIM drain uses **Settings → Missions → Mission Run** / per-run ``MissionRunOperatorDisplaySettings/simBatteryDrainRateDuringRun`` instead — not this Live Drive default.
- **Settings → Missions → Mission Run** — **app defaults only** on ``GeneralSettingsStore`` (`missionControlLiveMapHideOtherTasksOnTaskSelect`, default on; `missionRunSimBatteryDrainRate`, default normal with optional **None**; `missionRunResetSitlToStartPoseOnSuccessfulComplete`, default off). New runs **copy** these into ``MissionRunEnvironment/operatorDisplaySettings`` (``MissionRunOperatorDisplaySettings``) at ``MissionControlStore/createRun(from:cloningMissionRunDefaultsFrom:)``. **Mission Control Setup → Settings** and MC‑R **Settings** (sub-bar **switch.2**) edit **that run’s** ``operatorDisplaySettings`` — not ``GeneralSettingsStore``. Changing app defaults does **not** rewrite existing runs.

### Mission Run settings — defaults vs per-run envelope

- **App defaults (General Settings only):** **Settings → Missions → Mission Run** edits ``GeneralSettingsStore`` (`missionControlLiveMapHideOtherTasksOnTaskSelect`, `missionRunSimBatteryDrainRate`, `missionRunResetSitlToStartPoseOnSuccessfulComplete`). These are the **template** for **new** runs only.
- **Per-run (Mission Control):** When ``MissionControlStore/createRun(from:cloningMissionRunDefaultsFrom:)`` creates a ``MissionRunEnvironment``, it assigns ``operatorDisplaySettings`` from a **snapshot** of those app Mission Run fields at create time. **MCS Setup → Settings**, **MC‑R → Settings** (drawer), and **any Mission Control decision** (map isolation, **SIM battery drain while running**, SIM home reset gating, etc.) must read **``MissionRunEnvironment/operatorDisplaySettings``** — **never** ``GeneralSettingsStore`` for those behaviours on an active run.

| Setting | App default (`GeneralSettingsStore`) | Per-run value (`MissionRunEnvironment/operatorDisplaySettings`) |
| --- | --- | --- |
| **Isolate map to selected task** | `missionControlLiveMapHideOtherTasksOnTaskSelect` | `isolateLiveMapToSelectedTask` |
| **SIM battery drain while run executes** | `missionRunSimBatteryDrainRate` | `simBatteryDrainRateDuringRun` |
| **Reset SIMs when run completes** | `missionRunResetSitlToStartPoseOnSuccessfulComplete` | `resetSimToStartPoseOnSuccessfulComplete` |

- **Persistence:** Runs are **in-memory** for v1. If a **saved run snapshot** codec is added later, **encode** ``operatorDisplaySettings`` in that envelope; **decode** missing keys using the same defaults as ``MissionRunOperatorDisplaySettings`` init / legacy migration (see ``MissionRunOperatorDisplaySettings`` `Codable` for the old `liveMapIsolateOnTaskSelect` key).
- **Contributor rule:** When adding a new Mission Run–scoped setting, put the **app default** on ``GeneralSettingsStore`` (or a dedicated defaults type), **clone into** ``MissionRunOperatorDisplaySettings`` (or an adjacent run envelope struct) at **run create**, bind MCS / MC‑R UI to the **run**, and branch execution on the **run** field — do not read ``GeneralSettingsStore`` from Mission Control runtime paths for that setting.

| Layer | UI | Data |
| --- | --- | --- |
| **App defaults** | Settings → Missions → Mission Run | ``GeneralSettingsStore`` |
| **Per-run** | MCS Setup → **Settings**; MC‑R → **Settings** drawer | ``MissionRunEnvironment/operatorDisplaySettings`` |

**Extension:** Keep **MCS Settings** and the **MC‑R Settings** drawer aligned on the same ``MissionRunEnvironment/operatorDisplaySettings`` fields.

### Mission Run SIM clean up (product lock)

**Park** recipe: ``recipe.fleet.vehicle.do.park`` (``FleetMissionRecipeRegistrations/vehicleDoParkRecipeName``).

- **Scope:** Guardian-managed **SITL** streams only (``FleetLinkService/isGuardianManagedSitlStream``); never target live MAVLink aircraft with park or teleport cleanup.
- **When:** Canonical hook is ``MissionRunLifecycleSubsystem/markCompleted`` (``MissionRunStatus/completed`` + ``MissionRunSessionPhase/completed``). Today the operator path is **MC‑R / Mission Control Setup** “Mark Completed”, which calls ``markCompleted(kind:)`` from ``MissionControlSetupView``; any new completion UI **must** use the same lifecycle API so cleanup is not bypassed. Operators may also start the same pass manually from **Mission Control Setup → Tasks** (staging sidebar) via the **SIM cleanup** button while simulation is enabled.
- **Default behaviour (target):** **Step 1 — Park:** invoke ``recipe.fleet.vehicle.do.park`` for **every** in-scope SIM on that transition (**no** ``MissionRunCompletionKind`` gate for park — SIM motion must stop). **Step 2 — Teleport** toward stored start poses **only** when this run’s ``MissionRunEnvironment/operatorDisplaySettings/resetSimToStartPoseOnSuccessfulComplete`` is **on**; the **teleport** step may retain existing ``MissionRunCompletionKind`` / ``MissionRunSimHomeRestorePolicy`` gating until product explicitly removes it.
- **In-scope vehicles:** All **roster** assignments that resolve to a Guardian SITL stream, plus **floating reserve pool** slots that resolve to Guardian SITL — reuse MCS staging-map patterns (``MCSReservePoolHomeStagingMapEligibility``, ``MissionControlSetupView`` SIM drag / token → SITL resolution).

- **Phase A (shipped):** On ``markCompleted``, after MC‑R Live Drive **assignment** handoff flags clear, ``MissionRunEnvironment/cancelPendingCommandBatchesAndFleetHintsAfterRunCompleted`` revokes graceful after-cycle intent, cancels **every** pending MRE executor batch (all tags / dispatch kinds), logs when batches were removed, and clears ``FleetLinkService`` Live Drive **control session** plus MC‑R **park await-continue** latches. ``MissionRunSchedulingSubsystem/cancelAllScheduledTasks`` clears deferred task-start timers and **one-off deferred mission start** state. At the **start** of ``MissionRunEnvironment/scheduleMissionRunSimCleanupIfNeeded`` (including a **motion-only** pass when park and teleport are otherwise skipped but Guardian SITL sessions exist), ``FleetLinkService`` applies a best-effort per-vehicle **motion damp** — manual stream stop, ``Mission/pauseMission()`` (skipped for **PX4 UGV** offboard park, same as the park recipe), then offboard stop — logged with ``MissionRunLogTemplateKey/guardianSitlMotionStopPassAfterRunCompleted`` when any vehicle was targeted.

- **Phase B (shipped):** After the run-completed notification, the same coordinator continues (roster order, then floating reserve pool per ``MissionRunSimCleanupParkPolicy`` + ``MCSReservePoolHomeStagingMapEligibility``) with ``recipe.fleet.vehicle.do.park`` on every in-scope Guardian SITL via ``MissionRunCommandSubsystem/awaitRecipeDispatchAppendingDispatchedThenAckLogs`` (``FleetRecipeRunner`` + ``MissionRunRecipeOperatorPromptBridge``; issuer key ``MissionRunCommandIssuerKey/runCleanupPark``), **mission clear** on the **cleanup union**, and **SIM battery** patches — each in **waves** of up to **20** concurrent awaits per wave (``MissionRunSimCleanupConcurrency``), then the next wave until all vehicles in that step are done. Override the wave size for experiments with environment variable ``GUARDIAN_MISSION_RUN_SIM_CLEANUP_MAX_CONCURRENT`` (integer ≥ 1; see ``MissionRunSimCleanupConcurrency/envKey``). Logs ``MissionRunLogTemplateKey/lifecycleSimCleanupParkBatch``, then optional SIM home teleport when ``MissionRunSimHomeRestorePolicy`` passes (``missioncontrol.run_complete_sim_cleanup.teleport``; roster batch then pool batch, each logged with ``MissionRunLogTemplateKey/lifecycleSimHomeRestoreBatch``) — **skipping teleport** for any vehicle id whose park step failed. Roster/pool **teleport** batches themselves stay **sequential** (roster then pool); only park / mission clear / battery use waved overlap across **distinct** vehicle streams.
- **Executor (shipped):** ``MissionRunExecutionSubsystem/enqueueCommandBatch`` drops new ``MissionRunCommandQueueTag/missionStart`` work once ``MissionRunSessionPhase`` is ``completed`` (``MissionRunLogTemplateKey/executorMissionStartBatchSuppressedRunCompleted``) so mission-start batches cannot appear after the operator completes the run.

- **Operator feedback:** Partial outcomes (park failures; roster or pool home restore skips when snapshots exist) raise a **warning** toast through ``GuardianMissionRunSimCleanupOperatorToastNotification`` (handled next to reserve-swap toasts in ``GuardianHQApp``). A second cleanup request while ``MissionRunEnvironment/isMissionRunSimCleanupPassRunning`` is true shows an **info** toast instead of stacking passes.

### SIM home reset on Mission Control run complete (v1 — product locked)

Optional behaviour: when enabled for **this run** in **Mission Control Setup → Settings** or **MC‑R → Settings** (``MissionRunEnvironment/operatorDisplaySettings/resetSimToStartPoseOnSuccessfulComplete``, cloned from app defaults at run create), return bound **SITL** vehicles toward a captured **start pose** after a qualifying run end. **Settings → Missions → Mission Run** sets only the **default** used for **new** runs.

- **SITL only:** Live MAVLink aircraft are never auto-teleported; the same policy family as ``FleetLinkService/applySimState``.
- **Trigger:** After ``MissionRunLifecycleSubsystem/markCompleted`` leaves the run in ``MissionRunSessionPhase/completed`` (never for ``markFailed`` / aborted session). The stored ``MissionRunCompletionKind`` must satisfy ``MissionRunCompletionKind/qualifiesForSimHomeRestoreAfterSuccessfulMissionRun`` (operator **Complete** / graceful complete paths and ``oneOffAutopilotFinished``). **Operator Stop** outcomes and a **missing** ``completionKind`` do **not** qualify. Mid-run pause, abort-only paths, and Live Drive handoff are unchanged unless product revisits them.
- **Concurrency (v1):** Cleanup runs on the main actor **after** lifecycle completes into ``completed`` and the run-completed operator notification is shown, so **Completed** precedes SIM work. **Motion damp** then **park** run first (``scheduleMissionRunSimCleanupIfNeeded``); **teleport** is the same async continuation when the run’s reset pref and completion kind qualify. One coordinator — no duplicate apply batches per completion. The pass logs **start** and **finish** summary lines (``MissionRunLogTemplateKey/lifecycleSimCleanupRunStarted`` / ``lifecycleSimCleanupRunFinished``) around park, mission clear, teleport, and battery steps.
- **Apply (v1):** When the **run’s** reset pref is on and snapshots exist, the same ``scheduleMissionRunSimCleanupIfNeeded`` continuation runs **mission clear** (``fleetVehicleDoMissionClear``) then ``FleetLinkService/applySimState`` calls (`missioncontrol.run_complete_sim_cleanup.teleport`) **after** waved **park** on the cleanup union: **roster** candidates first, then **floating reserve pool** candidates (each batch logs ``MissionRunLogTemplateKey/lifecycleSimHomeRestoreBatch`` with applied / skipped / candidate counts). Rows without a resolved SITL stream, non-SITL streams, unknown autopilot stack, or **failed park** for that vehicle id are skipped. **SIM battery:** ``FleetLinkService/applySimBatteryFullChargeAfterRunCleanup`` (`missioncontrol.run_complete_sim_cleanup.battery_full`) runs on the cleanup vehicle set in the same **waved** pattern as park/mission clear.
- **Toggle scope:** **App:** ``GeneralSettingsStore/missionRunResetSitlToStartPoseOnSuccessfulComplete`` (defaults for new runs). **Mission Control:** ``MissionRunEnvironment/operatorDisplaySettings/resetSimToStartPoseOnSuccessfulComplete`` for gating — do not read the app store in MC runtime for this decision.
- **Pose apply stack (v1):** **Only** ``FleetLinkService.applySimState`` (pose / alt / yaw via ``FleetSimState``). There is **no** dedicated post-teleport **recipe** for PX4 battery or forced disarm; use **SIM Live Settings** and normal arm/disarm if stock PX4 SITL battery behaviour while **disarmed** looks unexpected after a reset (PX4 / ArduPilot drain parameters are documented under **What Guardian sets** below).
- **Start pose (v1 definition):** **Roster:** on the **first** ``MissionRunExecutionSubsystem/startExecution`` for this run, capture **fleet hub telemetry** into ``MissionRunEnvironment/rosterSimStartPoseSnapshotByAssignmentID`` (``FleetSimState`` per ``MissionRunAssignment/id``) for each roster row bound to a **Guardian SITL** stream with a valid lat/lon. **Floating reserve pool:** ``MissionRunEnvironment/reservePoolSimStartPoseSnapshotBySlotID`` (keyed by ``MissionRunReservePoolSlot/id``) fills **incrementally** on later ``startExecution`` calls for each eligible pool berth (same SITL / stack gates as ``MCSReservePoolHomeStagingMapEligibility`` + Guardian-managed stream): prefer hub lat/lon when present, otherwise MCS **bulk pool home** lat/lon (``reservePoolBulkSimHomeByTaskID``) with hub alt/yaw when available. v1 does **not** read the MC staging overlay for roster — hub truth at capture time. Snapshots drop when the mission template is cleared, rows/slots disappear, pool structure is replaced, or a row/slot’s fleet binding changes (planner / roster swap / pool replace / floating swap commit).
- **Back to setup from Completed:** v1 keeps the **original** snapshot from the first **setup → running** transition; returning to setup does not re-capture poses.
- **Tests (contributors):** ``MissionRunSimHomeRestorePolicyTests``, ``MissionRunCompletionKindSimHomeRestorePolicyTests``, ``FleetSimStateSimHomeRestoreSnapshotTests`` (hub + reserve-pool merge), ``MissionRunRosterSimStartPoseSnapshotInvalidationTests`` (roster + pool snapshot lifecycle), ``MissionRunSimCleanupParkPolicyTests``, ``FleetLinkServiceApplySimBatteryCleanupTests``, ``MissionRunSimCleanupOperatorToastCopyTests``.
- **Optional later:** Per-vehicle log lines beyond the batch + pass summaries; SITL smoke harness.

| Area | Primary symbols / files |
| --- | --- |
| Run complete + manual SIM cleanup | ``MissionRunLifecycleSubsystem``, ``MissionRunEnvironment`` (``markCompleted``, ``scheduleMissionRunSimCleanupIfNeeded``, ``canScheduleMissionRunSimCleanupNow``, ``isMissionRunSimCleanupPassRunning``, ``cancelPendingCommandBatchesAndFleetHintsAfterRunCompleted``, ``completionKind``), MC‑R “Mark Completed” and **SIM cleanup** on MCS **Tasks** staging sidebar in ``MissionControlSetupView.swift`` |
| Gating | ``MissionRunSimHomeRestorePolicy``, ``MissionRunCompletionKind/qualifiesForSimHomeRestoreAfterSuccessfulMissionRun`` |
| SIM apply | ``FleetLinkService.applySimState``, ``FleetSimState`` |
| Snapshots + SIM cleanup logs + operator toasts | ``MissionRunEnvironment/rosterSimStartPoseSnapshotByAssignmentID``, ``reservePoolSimStartPoseSnapshotBySlotID``, ``captureRosterSimStartPoseSnapshotsIfNeeded``, ``clearRosterSimStartPoseSnapshots(forAssignmentIDs:)``, ``clearReservePoolSimStartPoseSnapshots(forSlotIDs:)``, ``MissionRunLogTemplateKey/lifecycleSimCleanupRunStarted``, ``lifecycleSimCleanupRunFinished``, ``lifecycleSimHomeRestoreBatch`` (``phase`` param), ``GuardianMissionRunSimCleanupOperatorToastNotification`` |

- **LiveDrive** — When a **SIM** is selected, use the **cog** in the sub-bar to open **SIM Live Settings**: toggle drain for that vehicle, pick a rate, and **Apply**. The sidebar explains PX4 vs ArduPilot behavior at a glance.

### What Guardian sets

| Stack      | Parameter          | When drain is **on** | When drain is **off** |
| ---------- | ------------------ | -------------------- | --------------------- |
| **PX4**    | `SIM_BAT_DRAIN`    | Positive float: full-discharge time in **seconds** (while the vehicle is **armed** in stock PX4 SITL). | `0` (disables the timed drain model). |
| **ArduPilot** | `SIM_BATT_CAP_AH` | Positive float: simulated pack capacity (**Ah**) for the SITL battery integrator. | `0` (static / non-integrating model for that path). |

Successful writes appear in the fleet log as `Param set [source=…] …` for the vehicle; failures show as `Param set failed …`.

### Autopilot semantics (why it can still read “100%”)

These rules come from the **simulators**, not from Guardian skipping a step.

- **PX4** — Stock `BatterySimulator` **only runs the discharge timer while armed**. While **disarmed**, it **resets** the simulated state of charge toward **full**. So LiveDrive or MC-R with a disarmed PX4 SIM will often look stuck at 100% even though `SIM_BAT_DRAIN` is set.
- **ArduPilot** — SITL battery charge moves with **integrated current**. **Near-zero motor current** (disarmed or idle throttle) means **little or no** visible drop for a long time.

To validate the feature: **arm** a PX4 SIM (or apply meaningful throttle on ArduPilot) and watch battery telemetry; confirm params in the log if in doubt.

## SIM state on the wire (`FleetSimState` / `applySimState`)

Guardian routes **every** programmatic SIM pose/SIM-parameter reset through one API so spawn, Mission Control‑E (MC‑E), and future “SIM recovery” actions stay aligned.

### Types

| Symbol | Location | Purpose |
| ------ | -------- | ------- |
| `FleetSimState` | `Sources/GuardianHQ/FleetSimState.swift` | Values to apply: lat/lon, optional AMSL-ish altitude, yaw, optional ArduPilot `SIM_BATT_*`, optional PX4 `SIM_BAT_DRAIN`. |
| `FleetLinkService.applySimState(vehicleID:state:autopilotStack:source:)` | `FleetLinkService.swift` | **Only** supported path to push those values to the autopilot over MAVSDK (built-in **Guardian SITL** streams only). |

Builders in use today:

- `FleetSimState(spawnDefaults:)` — mirrors **General → SIM spawn defaults** when reinforcing state after link-up (`source` typically `sitl.spawnHandshake`).

Other call sites build `FleetSimState` manually (or add new `init` helpers) then call `applySimState`.

### Autopilot wiring (what gets set)

**ArduPilot SITL:** `SIM_OPOS_LAT`, `SIM_OPOS_LNG`, `SIM_OPOS_ALT`, `SIM_OPOS_HDG`; optionally `SIM_BATT_VOLTAGE`, `SIM_BATT_CAP_AH` when the `FleetSimState` carries values.

**PX4 SIH SITL:** `SIH_LOC_LAT0`, `SIH_LOC_LON0`, `SIH_LOC_H0`; optionally `SIM_BAT_DRAIN` when set on state.

Launch recipes (`SitlLaunchRecipe`) still pass **CLI/env seeds** (`sim_vehicle.py -l …`, `PX4_HOME_*`); after MAVSDK reports **connected**, the spawn path schedules one `applySimState` (~0.5s delay) so Guardian and the autopilot agree on pose without duplicating unrelated launch logic in Swift.

### Guidance for Mission Control‑E (and agents)

When MC‑E (or tooling) needs to **teleport or reset SIM-facing params**:

1. Build a `FleetSimState` with the desired fields (reuse existing builders or populate manually).
2. Resolve `FleetAutopilotStack` for the vehicle (from fleet model / telemetry, same as other fleet code).
3. `await fleetLink.applySimState(vehicleID:vID, state:, autopilotStack:, source:"mc.phaseName")`.

Do **not** add parallel `setVehicleFloatParameter` call sites for the same SIM_OPOS / SIH_LOC family—extend `FleetSimState` / `applySimState` instead so UX and logs stay coherent.

## Live Drive control session (freestyle & mission, SIM & live)

While a Live Drive session is active, `FleetLinkService` sets **`liveDriveControlSessionVehicleID`** (`setLiveDriveControlSessionVehicle` / `clearLiveDriveControlSessionVehicleIfMatches`). Only that vehicle may receive **`liveDrive.*`** commands with **`.manualTakeover`** or run **manual control streaming** (`startManualControlStream`, `updateManualControlIntent`). This applies to **SIM and real aircraft**; **freestyle** and **mission** handoff both use the same gate (session kind is stored on `LiveDriveSessionRecord` for export). Param-only paths (e.g. SIM battery drain from the cog) are not gated by this.

**Mission session start (vehicle still on a Mission Control run):** Tapping **Start session** runs ``MissionRunEngageStabilizeTelemetryClassifier/evaluateLiveDriveMissionStartStabilizeGate`` first (park-stable for UGV/USV/UUV; **loiter- or park-stable** for UAV — same classifier family as MC-R Engage stabilize watch). If the verdict is not ``stable``, the app surfaces a toast and **does not** open the arm preflight sheet. When **stable**, ``VehiclePreflightSheet`` calls ``MissionControlStore/runSingleVehiclePreflightProbe`` with ``allowDuringLiveMission: true``; on pass, the same flow as today continues into manual streaming (operator gesture end-to-end — no unattended start).

**Return to Mission Control (mission handoff):** Ending a Live Drive session clears the control session, restores the **missionControl** command authority gate, and clears MRE assignment handoff (``missionRunAssignmentIDsWithOperatorLiveDriveHandoff``) so autonomous paths can run again **for that slot**. Session end uses the existing class-aware end sequence (e.g. surface **hold + disarm** or **RTL → park** from ``LiveDriveView`` / ``FleetLinkService``) so the vehicle is left in a **park- or loiter-like** posture as appropriate — **not** by auto-resuming the autopilot mission. The app **does not** automatically queue **Continue mission** / ``missionStart``; the operator returns to MC‑R and taps **Continue mission** (or equivalent catalogue steps) when they are ready. **Engage drill-in:** ``OperatorPromptReviewFocusController/requestLiveDriveEngageDrillIn`` switches to the Live Drive section, seeds ``pendingLiveDriveVehicleID`` (consumed after ``LiveDriveStore/selectVehicle``) and keeps ``pendingLiveDriveMissionRunID`` until session start succeeds/fails or the session ends so ``OperatorPromptCenter/setLiveDrivePromptPanelHostContext`` can attribute prompts **before** **Start session**.

## Mission roster behavior roles (Missions v1)

**Behavior roles** (`RosterDevice.behaviorRoleID`, usually a ``RosterRole`` slug) describe how MRE should bias a device on the mission roster. They are **not** the same as **slot** roles (`MissionRosterSlotRole`: primary / wingman / reserve).

| Concern | Owner |
| --- | --- |
| Catalog (display copy, tags, default weights), persistence on the template | **Missions** |
| Plugin tag/weight **overlays** on built-in roles | **Missions** — `RosterRoleExtensionRegistry.registerOverlay` (`@MainActor`); each `GuardianPluginID` may contribute at most one overlay per `RosterRole`. |
| Plugin **full catalog rows** (any stable `role_id` string) | **Missions** — `RosterRolePluginCatalog.register` (`@MainActor`); **last write wins** per `id` (later registration replaces any prior row for that slug, including from another plugin). Re-registering `id` equal to `RosterRole.none.rawValue` is ignored. |
| Template persistence for arbitrary `role_id` | **`RosterDevice.behaviorRoleID`** (JSON key still `role`); any non-empty string round-trips. Built-in enum cases use their `RosterRole.rawValue` slugs. |
| Run-time resolution / logging | **Mission Control** — `MissionRunRosterRoleResolver` + `MissionRunEnvironment.rosterRoleResolutionsByDeviceID`; MC execution start logs a one-line behavior-role snapshot (template key `rosterBehaviorRolesSnapshot`). Paladin does not branch on roster roles; resolved payloads are available on the run for future consumers. |

**Implementation (shipped):**

- `Sources/GuardianHQ/Systems/Missions/Models/RosterRoleCatalog.swift` — built-in eight roles + `none`; `RosterRoleDefinition`, `RosterRoleWeights`, `RosterRoleMREPayload` (JSON keys `role_schema`, `role_id`, `tags`, `weights`). `RosterRoleCatalog.mrePayload(forBehaviorRoleID:)` is `@MainActor`, returns `nil` only for the `none` slug, prefers **plugin full rows** when registered for that id, otherwise merges **plugin overlays** on built-ins via `RosterRoleExtensionRegistry`. `mrePayload(for: RosterRole)` delegates to the string id path.
- `Sources/GuardianHQ/Systems/Missions/Models/RosterRoleExtensionRegistry.swift` — `RosterRolePluginOverlay`, `RosterRoleWeightDeltas` (per-knob deltas summed across plugins, total clamped to ±0.25 per knob before adding to built-in weights, then 0…1); `resolvedDefinition(for:)` exposes merged tags, weights, and contributing plugin IDs (for audit / export).
- `Sources/GuardianHQ/Systems/Missions/Models/RosterRolePluginCatalog.swift` — `RosterRolePluginCatalogEntry` + `RosterRolePluginCatalog.register` for open-set definitions; `RosterRoleCatalog.mrePayload(forBehaviorRoleID:)` prefers a plugin row over built-in + overlays when the slug matches.
- `Sources/GuardianHQ/Systems/MissionControl/Models/MissionRunRosterRoleResolution.swift` — `ResolvedRosterRole`, `MissionRunRosterRoleResolver` (per-device DTO + `mrePayload`); avoids MC re-parsing overlay merge rules ad hoc.
- `Sources/GuardianHQ/Systems/MissionControl/Services/MissionRun/MissionRunEnvironment.swift` — keeps `rosterRoleResolutionsByDeviceID` in sync with the mission template / execution context.
- `Sources/GuardianHQ/Systems/Missions/Models/Mission.swift` — `RosterRole` enum (raw values = stable slugs); `RosterDevice` stores `behaviorRoleID` and JSON-decodes the `role` / legacy `character` keys into that string (unknown slugs are preserved).
- `Sources/GuardianHQ/Systems/Missions/Views/MissionsView.swift` — role pickers list built-in slugs plus any plugin-registered ids; display names / blurbs resolve through ``RosterRoleCatalog/displayName(forBehaviorRoleID:)`` and ``blurb(forBehaviorRoleID:)`` (plugin row copy when present).
- Tests: `RosterRoleCatalogTests`, `RosterRoleExtensionRegistryTests`, `RosterRolePluginCatalogTests`, `MissionRunRosterRoleResolutionTests`.

Tag bundles and overlay rules are defined in `RosterRoleCatalog` / `RosterRoleExtensionRegistry` and covered by `RosterRole*` tests. Optional roster UI polish (e.g. icons per role cluster) is general Missions work.

## Mission template points (v0 model)

Typed **map points** (rally, extraction, …) live on the mission as **`Mission.missionPoints`** — not task path waypoints. During Mission Control, **`MissionRunEnvironment.runtimeMissionPoints`** holds the **live run envelope** (seeded from the template in setup; after the run leaves setup, template refresh does **not** replace the envelope — only create/update/close APIs mutate it). Edit **template** points on the mission **Tasks** tab (**Map points** card + map) or in **Mission Control Setup → Tasks** (segmented **Tasks | Points**). While a run is **live** (MC-R), use the live map **map points** control to open the **Map points** panel on the Tasks card — add / edit / move / close rows against the **runtime** envelope only (`applyRuntimeMissionPointCreate` / `Update` / `SetClosed`). **Deferred:** automatic reroute / RoE prompts when a live point **moves** or **closes under active use** requires executor / observer work — see **`NEXTVERSION.md`** → **Mission map points: live RoE / replan when coordinates or closed state change (MCR / MRE)** (2026-05-11).

| Field / rule | Notes |
| --- | --- |
| Storage | Single array on **`Mission`**; each **`MissionPoint`** has **`taskID: UUID?`** (`nil` = mission-wide). |
| **`pointId`** | Stable slug within the mission for MRE / recipes / logs (distinct from row **`id`**). |
| **`catchmentRadiusM`** | **1…1000** m, default **10**; clamped on init and decode. |
| **`isClosed`** | Soft-retire flag; MC-R operator can toggle from the **Map points** panel or edit drawer. |
| Task delete | Removing a **`MissionTask`** removes points whose **`taskID`** matches that task. |
| Clone | **`MissionStore.cloneMission`** duplicates points with new row **`id`** and same **`pointId`** (mission id namespaces the file on disk). |

**Mission Preflight (Mission Control setup → run):** ``MissionRunStartPreflightOverlay`` runs arm probes on **roster** assignments only (`MissionRunEnvironment/orderedStartRunPreflightProbeTargets()` defaults `includeFloatingReservePoolSlots` to `false`). **Floating pool** aircraft are **not** probed at start run so reserves do not block mission start; at **MC-R reserve swap confirm** the store runs **telemetry-only** hub snapshot gates (``MissionControlReserveSwapInPreflightGates`` via ``MissionControlPreflightTelemetryGateMode/reserveSwapIn`` — no catalogue / recipe audit) then the **arm probe** recipe with `allowDuringLiveMission: true` and audit source `missionControl.preflightProbe.reserveSwapIn` (single catalogue attribution for the arm step). Pass `includeFloatingReservePoolSlots: true` only for diagnostics. Empty pool berths are never listed. Like roster rows, the probe path requires a parsable **`FleetMissionVehicleToken`** storage key; legacy text-only bindings surface the same “pick from the fleet list” failure as roster slots without a token.

## Floating reserve pool (Mission Control run)

Optional **per-task floating reserve pool** lives only on the **run envelope** (`MissionRunEnvironment.reservePoolByTaskID`), same persistence class as `runtimeMissionPoints` — **not** new fields on `Mission` / `MissionTask` / `RosterDevice`. Pool rows are **slots** (berths): `MissionRunReservePoolSlot` / `MissionRunReservePool` in `MissionRunReservePool.swift`; APIs on `MissionRunEnvironment` include `appendReservePoolSlot`, `replaceReservePoolSlot`, `returnAssignmentToReservePool`, `availableReservePoolEntries`, `writtenOffFleetVehicleStorageKeysForReservePool`, and template prune in `updateTemplate`.

**Mission / slot policy state (inventory):** A pool slot stores **label + binding** only (`attachedFleetVehicleToken` / `attachedDevice`); it does **not** carry mission-policy execution state (which abort/complete tactic ran, recipe success, “slot done”). The same separation applies to **roster** ``MissionRunAssignment`` — see **Mission run state model** → **MissionRunAssignment (roster row model)**. Fleet **operational** bars (lifecycle stage, battery band, written-off keys on the run) gate **draw / return / swap** via ``MissionRunEnvironment`` + ``FleetVehicleOperationalModel`` helpers — they are not duplicated as columns on the pool row. When per-**assignment** or per-**pool berth** lifecycle enums land (``TaskRosterAssignmentStatesToDo.md`` §2), choose **one** evidence map keyed consistently (e.g. ``assignment.id`` for roster rows, pool ``MissionRunReservePoolSlot/id`` for berths, or a single parallel ``[UUID: SlotState]`` on the run) so MRE does not fork two sources of truth for “policy satisfied.”

| Decision | Locked behaviour |
| --- | --- |
| Squad scope | **One pool per task**, shared by every squad on that task. |
| Re-consumption | Replaced primaries/wingmen may **return to the pool** when still functional; **battery / operational draw** gates match `returnAssignmentToReservePool` / `qualifiesForMissionRunReservePoolOperationalDraw` when `fleetLink` + `sitl` are attached. |
| MRE random draw | **Uniform random** among eligible filled slots; **no** dedicated RNG seed on the run. |
| Reserve swap candidate union | ``MissionRunReserveSwapCandidate`` (`typealias` ``ReserveSwapCandidate`` in ``MissionRunReservePool.swift``): ``floatingPool(taskID,slot:)`` or ``fixedRosterReserve(assignment:)``; enumerated by ``MissionRunEnvironment/enumerateReserveSwapCandidates(vacancyAssignmentID:taskID:ordering:)`` with ``MissionRunReserveSwapCandidateOrdering`` — default ``poolSlotsFirst`` (MC-R / operator pickers); ``fixedRosterReservesFirst`` for autonomous / headless callers that walk the array in order. ``MissionRunReserveSwapRankingPolicy/pick(from:)`` takes ``[ReserveSwapCandidate]``. **Wingman vacancy — fixed reserve order:** reserves whose ``RosterDevice/leaderRosterDeviceId`` matches this wingman’s primary first (explicit wingman link, or inferred when the task has exactly one primary), then **auto** reserves (`leaderRosterDeviceId == nil`), then reserves tied to another primary; within each band: ``MissionRunAssignment/slotName`` then ``id``. **Primary** vacancies: fixed reserves sort by ``slotName`` then ``id`` only. |
| Hull written-off | **Vehicle** (fleet storage key) set on the run excludes draws: `markFleetVehicleWrittenOffForReservePool` / `clearFleetVehicleWrittenOffForReservePool`. |
| Class substitution | ``FleetVehicleType/substitutionMatches(required:candidate:policy:)`` and ``FleetVehicleSubstitutionPolicy`` in ``FleetVehicleModel.swift``: ``exactGranularType`` is equality only (``unknown`` matches ``unknown``). ``missionRunReserveSwap`` keeps that and additionally allows **UGV-Wheeled ↔ UGV-Tracked**; other kinds stay exact. Typed vacancy + ``unknown`` candidate (or ``unknown`` vacancy + typed candidate) ⇒ **no** match. **MRE draw / swap:** ``MissionRunEnvironment/availableReservePoolEntries(forTaskID:classCompatibleWithAssignmentId:)`` and ``swapRosterAssignmentWithRandomFloatingReserve`` filter pool rows against the assignment’s template ``RosterDevice/vehicleClass``; template ``unknown`` does not apply a class filter. Legacy text-only pool rows are excluded when the vacancy is typed and fleet services can resolve types. **Candidate union (swap-in prep):** ``MissionRunEnvironment/enumerateReserveSwapCandidates(vacancyAssignmentID:taskID:ordering:)`` returns ``MissionRunReserveSwapCandidate`` rows — class-gated pool slots plus same-task template ``.reserve`` roster bindings (operational / written-off rules aligned with pool draws). Default ordering is **pool then fixed** for MC-R; pass ``MissionRunReserveSwapCandidateOrdering/fixedRosterReservesFirst`` when automation should consider template bench rows before floating berths. Operator swap-in is **MC-R** (targeted berth); ``swapRosterAssignmentWithRandomFloatingReserve`` remains for draws/tests and commits **pool** rows only; fixed template reserves commit via ``MissionRunEnvironment/swapRosterVacancyWithFixedTemplateReserveAssignment`` (same pre-commit style as pool swap-in; invoked through ``MissionControlStore`` when headless assistants or other automation trigger engagement-backed swaps). |

**MCS (setup):** `MissionRunDetailView` / `MissionControlSetupView` — per expanded task, a horizontal **Floating reserves** strip (`ScrollView`), `+` adds a slot, each cell reuses `MissionControlRosterSlotCard`; duplicate fleet token warnings across pool + roster. Floating-reserve **swap-in** from a roster row is **MC-R only** (vehicle overlay / pool picker / live-console roster **⋯** → `beginMcrReserveSwapPick`; roster commit remains ``MissionRunEnvironment`` APIs), not a random-draw control on MCS roster cards. **Live Drive** has no dedicated swap-in control — reserve engagement there is **operator prompts** (e.g. ``MissionRunEngagementAction/swapInReserve`` / ``OperatorPromptRouter``) only. **Paladin** is **headless** (no UI): it calls Mission Control / MRE APIs only. Other **automation** likewise uses APIs, not product chrome as an entry point.

**MCS — Set reserve pool home (Rosters › Tasks staging map):** Task header overflow → **Set reserve pool home** arms a cursor-following preview; one map click sends the same lat/lon to each **eligible** floating-reserve pool SITL on that task (sequential ``applySimState``). The staging map **fits** immediately after apply, then **fits again after ~0.35 s** so hub-updated marker positions can widen the bbox. **Pool SIMs** with hub lat/lon also render as **non-draggable** map markers (label `… · pool`, ids ``MissionControlReservePoolMapMarkerID`` — same scheme as MC-R); tap expands that task’s roster accordion. Each placement attempt appends a run log line (``MissionRunLogTemplateKey/mcsReservePoolHomeMapBatch``). **New berths or rebound** pool aircraft do **not** automatically inherit a prior bulk hub; run **Set reserve pool home** again. If the **mission template vanishes** from the store or the **armed task is disabled or removed** while placement mode is on, the mode **clears** automatically. Operator copy stays expectation-setting (telemetry may lag); cancel via the map notice, Esc, the menu affordance, or leaving Rosters / Tasks.

**Paladin — fixed template reserve proposal + commit (v1):** ``PaladinMissionAssistant/raiseSwapInReservePrompt`` forwards to ``MissionControlStore/raiseOperatorPromptSwapInReserve`` with an explicit ``OperatorPromptDisplaySource/assistant`` for Paladin (observer needs ``MissionRunObserverPermissions/act``). **Assistants do not implement swap gates or alternate commit pipelines** — they call the same Mission Control store path as any headless issuer; the store applies ``MissionRunEnvironment/swapRosterVacancyWithFixedTemplateReserveAssignment`` after engagement rules (the same MRE roster primitive operators use once MC-R exposes fixed-reserve swap-in). ``MissionRunPaladinReserveSwapProposalPolicy`` validates the **primary or wingman** vacancy and a **template `.reserve`** roster row on the **same enabled task**, both with non-empty ``attachedFleetVehicleToken`` (floating **pool** picks stay on the MC-R swap UI, not this API). ``MissionRunEngagementAction/swapInReserve`` disposition: **forbidden** → warning log, `false`; **autonomous** → engagement log then immediate commit + plan recompile (when fleet services are attached), `true` only if the commit succeeds; **ask** / **defer** / **handoff** → Mission Control may register an **MC-R operator prompt** via ``MissionControlStore/raiseOperatorPromptSwapInReserve`` → ``MissionRunRecipeOperatorPromptBridge/awaitFixedReserveSwapEngagementConsent`` (Mission Control uses the caller-supplied ``operatorPromptDisplaySource`` for the **Source:** caption; it does **not** infer plugin identity from ``issuerKey``). After resolution, structured log lines record the verb; on **Acknowledge**, ``MissionControlStore/applyFixedTemplateReserveRosterSwapFromEngagementConsent`` runs the commit + ``recompileMissionControlPlanAfterFloatingReserveSwap`` with ``MissionRunReserveSwapPlanRecompilationPolicy/fixedRosterReserveSwapPlanCompileSource`` (Abort / timeout leaves the roster unchanged). Assistant log keys include ``paladinReserveSwapCommitted`` / ``paladinReserveSwapCommitRejected``; Mission Control logs ``MissionRunLogTemplateKey/fixedRosterReserveSwapEngaged`` on success.

**MC-R (live console):** When a class-compatible floating reserve exists for that roster slot, the live roster strip shows a **⋯** menu on the health card (same pick flow as **Swap in reserve** in the vehicle overlay). The pool-row confirm stays short; deeper behaviour is documented under **Floating reserve pool** above and in arm-check failure copy when a probe does not pass. **Engage Live Drive — stabilize (v1):** while the run status is **running**, **paused**, or **recovery**, the **roster vehicle** overlay and **floating reserve berth** overlay show **Park** (primary) and **Loiter** (outline) when the row has a non-empty ``MissionRunAssignment/attachedFleetVehicleToken`` and a resolved bridge ``vehicleID``; taps issue operator catalogue commands through ``MissionRunEnvironment/issueOperatorEngageStabilizeDispatch`` (same MRE dispatch + run log path as other fleet catalogue rows). After a successful queue, ``MissionRunEngageStabilizeTelemetryClassifier`` evaluates hub freshness (live vs SITL age limits aligned with ``MissionControlReserveSwapInPreflightGates``), park “on deck” heuristics aligned with the fleet park-wait signal, and loiter/hold-style modes (see ``FleetVehicleLiveStatusBadgeRow/isEngageLoiterLikeFlightMode``) plus vertical / horizontal rate hints; the overlay shows a non-blocking **Watching telemetry** / **Stabilized** / warning notice with **Dismiss**, and a timeout fault after **90s** if criteria never reach stable. **Engage — executor queue (v1):** after §1 stabilize, operator **Engage** calls ``MissionRunEnvironment/cancelPendingExecutorBatchesForOperatorLiveDriveEngage`` (``MissionRunExecutionSubsystem/cancelPendingCommandBatches`` for **every** ``MissionRunCommandQueueTag``) then ``noteOperatorLiveDriveHandoffActive`` for that assignment before drill-in to Live Drive; if anything was removed from the executor queue, the run logs ``MissionRunLogTemplateKey/executorPendingBatchesCancelledForLiveDriveEngage``. **Do not** surface **Engage** while ``reserveSwapPostCommit`` work must still complete — cancelling that tag can strand swap handoff. **Handoff clear:** ``MissionControlStore/clearOperatorLiveDriveHandoffForClearedControlSessionVehicle`` runs when the Live Drive control session clears for that bridge vehicle so MRE autonomous paths resume. **Return:** teardown does **not** auto-queue **Continue mission** / ``missionStart``; the operator ends Live Drive into a park/loiter-style posture (session end sequence), then explicitly taps **Continue mission** in MC‑R when ready — see **README** § Live Drive control session above. **Reserve auto-suggest (v1):** while the run is in ``MissionRunSessionPhase/executing`` with status **running** or **paused**, the focused task is in ``MissionTaskState/executing`` or ``between`` with **no** ``MissionTaskAttemptState`` wind-down row, and a **class-matched** pool candidate exists for a **primary or wingman** roster slot, Mission Control may emit a **debounced warning toast** (per fleet vehicle id) for: hub silence beyond **45 s**, a **recent fleet dispatch failure** log for that vehicle, battery traffic **warn**/**critical** (same bands as roster cards), or an RTL / return-home style **flight mode** substring match — copy points operators at **Swap in reserve**. **Reserve auto-swap executor (v1):** when ``MissionRunEngagementAction/swapInReserve`` is **autonomous**, the same distress signals fire, and ``MissionRunEnvironment/enumerateReserveSwapCandidates`` (with ``MissionRunReserveSwapCandidateOrdering/fixedRosterReservesFirst`` so a future sequential picker tries bench roster before pool) returns **exactly one** candidate (floating pool **or** fixed template reserve), MC-R runs the same **reserve arm preflight** probe as operator swap-in, then commits via ``swapRosterAssignmentWithFloatingReservePoolSlot`` or ``swapRosterVacancyWithFixedTemplateReserveAssignment`` (trigger `…reserveSwap.autoExecutor`, **300 s** debounce per vacancy). If engagement is not autonomous, or more than one candidate exists, only the suggest toast path applies (no unattended commit). Autopilot failsafe / STATUSTEXT classifiers are **not** wired into this path yet.

**Recipe escalation → reserve hint:** Only a matcher whose ``then`` is ``FleetRecipeControlOutcome/escalate`` with ``FleetRecipeEscalationReason/unrecoverableFailure`` and ``FleetRecipeUnrecoverableFailureKind/needsAirframeReplacement`` (JSON: `kind: escalate` → `reason.kind: unrecoverableFailure`, `reason.failureKind: needsAirframeReplacement`, plus `allowedVerbs`) triggers the MC-R augmentation path — not a free-form string or a different escalation branch. On a Mission Control run, ``MissionRunRecipeOperatorPromptBridge`` passes the live ``MissionRunEnvironment`` so the MC-R bottom operator banner (``MissionRunOperatorRecipePromptBanner``) adds **how many class-matched reserve candidates** exist for that vacancy (same enumeration as swap-in) and points operators at **Swap in reserve** when any exist. **Stage D router:** when the same shape is published as an ``OperatorPromptEvent`` with a ``missionRunID``, ``OperatorPromptRouter`` swaps the default recipe-escalation policy for an MCR-first channel list that **omits** the Vehicle Inspector wizard entry so reserve-swap guidance does not fall through to inspector chrome when MCR is unavailable.

**MRE (swap + plan):** ``swapRosterAssignmentWithRandomFloatingReserve`` / ``swapRosterAssignmentWithFloatingReservePoolSlot`` (pool pick via ranking policy or operator-chosen berth); **fixed template reserve ↔ vacancy:** ``swapRosterVacancyWithFixedTemplateReserveAssignment``. **Pre-commit (runtime dedupe):** immediately before mutating roster/pool, re-checks written-off set, duplicate fleet token on another roster row, duplicate token across pool berths, and reserve-pool operational draw on the **picked** reserve — failure yields ``MissionRunFloatingReserveSwapOutcome/pickRejectedDuplicateOrStaleBinding`` or the fixed-roster analogue ``MissionRunFixedRosterReserveSwapOutcome/pickRejectedDuplicateOrStaleBinding`` with **no** mutations. **Commit:** a **single in-place exchange** — the roster vacancy row takes the pool berth’s binding and that **same** pool berth row receives the vacancy’s prior binding (no new pool slots); vacating fleet bindings still pass the same eligibility gates as ``returnAssignmentToReservePool`` would use before the write. Then log template `missioncontrol.mre.reserve.swap_engaged`. **Plan mutations:** ``MissionRunPlannerSubsystem`` post-vets ``MissionControlPlanMutation`` so a fleet token cannot be roster-bound while still held in any pool berth (`reservePoolContainsFleetVehicleStorageKey`), and task reassignment cannot move an assignment onto a task whose pool already holds that token. **After a successful swap** (when the resolved mission is available), ``MissionControlStore/recompileMissionControlPlanAfterFloatingReserveSwap`` runs ``compileInitialPlan`` with source ``MissionRunReserveSwapPlanRecompilationPolicy/floatingReserveSwapPlanCompileSource`` for pool commits or ``fixedRosterReserveSwapPlanCompileSource`` for fixed roster commits: in **setup** it matches full ``compileMissionControlPlan`` (including ``markCompiled``); when the run has **left setup** it refreshes the compiled plan + log task context + ``refreshDerivedTaskStates`` **without** clearing logging state or calling ``markCompiled`` (session phase must not regress to ``compiled`` mid-execution). **Plugin assistants:** mutation commit callbacks (``MissionRunPlanningMutationAssistant`` `didApply`) fire from ``compileInitialPlan`` — see **Reserve swap plan recompilation + plugin assistants** below.

**Swap-time checklist (live reserve swap-in, v1)** — **policy only** until the swap pipeline executor wires gates end-to-end (remaining mechanics in ``MissionRosterReservesToDo.md``). Machine-readable lists live in ``MissionRunReserveSwapTimeChecklist`` (`MissionRunReserveSwapTimeChecklist.swift`).

| Phase | ArduPilot | PX4 | Unknown stack |
| --- | --- | --- | --- |
| **1 — Arm path** | Pick **one** of ``recipe.fleet.diagnose.armprobe`` **or** ``recipe.fleet.diagnose.armprobe.hold`` (same rule as Mission Control start-run probe — never both in one gate). | Same | Same |
| **2 — Optional calibration** | ``recipe.fleet.calibrate.compass`` → ``recipe.fleet.calibrate.accelerometer`` → ``recipe.fleet.calibrate.rc.trim`` (ordered; skip steps the operator or automation marks unnecessary). | Same | Same |
| **Non-recipe gates** | ``MissionRunReserveSwapTimeChecklist/NonRecipeGate`` — **RC link** (live calibration / hub health) and **geofence / fence** signals (preflight advisor classifiers on recent FC text). No stack-specific recipe split in v1. | Same | Same |

**Swap pipeline failure branches (v1)** — after a **gate** fails on the current reserve candidate (phases ``MissionRunReserveSwapPipelinePhase`` in `MissionRunReserveSwapFailureBranchPolicy.swift`). **Roster commit** and **post-commit executor phases** (``postCommitHandoff``, ``displacedMissionClear``, ``displacedFleetWindDown``, …) never auto-retry through this policy: any failure there yields ``MissionRunReserveSwapFailureDisposition/abortSwapPreserveCommittedState`` (operator / recovery path). Other phases: up to **``MissionRunReserveSwapFailureBranchPolicy/maxAutomaticRetriesAfterFirstFailureSameCandidatePerGate``** automatic replays on the **same** candidate (initial attempt + that many retries), then **``tryNextCandidate``** if enumeration still has another ranked option, else **``escalateToOperatorPrompt``** when a UI or blocking operator channel exists, else **abort** without further automatic work.

**Reserve stream ↔ ``FleetRecipeRunner`` (v1)** — ``MissionRunReserveRecipeRunnerCorrelation`` (`MissionRunReserveRecipeRunnerCorrelation.swift`) carries ``missionRunID``, ``missionTaskID``, **vacancy** ``vacancyAssignmentID``, **reserve** ``reserveStreamAssignmentID`` (pool slot id for ``MissionRunAssignment/syntheticForReservePool`` rows, else the fixed reserve assignment id), optional ``reservePoolSlotID``, and ``vehicleID``. ``MissionControlStore/runReserveSwapStreamRecipe`` forwards to ``FleetRecipeRunner/run`` with ``source:`` = ``MissionRunReserveRecipeRunnerCorrelation/recipeRunnerSource(phase:)`` (namespace ``missioncontrol.reserveSwap.<phase>``) and default ``allowDuringLiveMission: true``. MC-R escalations should key ``MissionRunRecipeOperatorPromptBridge`` targets off **reserve** vs **vacancy** ids depending on which airframe owns the failure.

**Reserve swap phase logging (v1)** — ``MissionRunReserveSwapPhaseLogTemplateKey`` (`MissionRunReserveSwapPhaseLogTemplateKey.swift`) defines keys `missioncontrol.mre.reserve.phase.<slug>.(pass|fail)` for each ``MissionRunReserveSwapPipelinePhase``, registered in ``StructuredLogTemplateCatalog``. Use ``MissionRunReserveSwapPhaseLogTemplateKey/templateParams(phase:correlation:detail:recipeRaw:)`` with ``appendLogEvent`` so export / MCR / Paladin share keys `phase`, `missionRunID`, `missionTaskID`, `vacancyAssignmentID`, `reserveStreamAssignmentID`, `poolSlotID`, `vehicleID`, `detail`, and optional `recipe`. **MC-R live swap-in** emits ``pickReserve`` and ``swapTimeChecks`` pass/fail around operator confirm + arm probe, and ``rosterCommit`` fail lines when the roster primitive returns a non-success outcome; successful **pool** commit still logs ``MissionRunLogTemplateKey/floatingReserveSwapEngaged`` then ``rosterCommit`` **pass** from ``MissionRunEnvironment/commitFloatingReservePoolPickToVacancy``; successful **fixed roster reserve ↔ vacancy** logs ``MissionRunLogTemplateKey/fixedRosterReserveSwapEngaged`` then ``rosterCommit`` **pass**. After plan recompile, ``beginPostCommitReserveSwapHandoffPipeline`` emits ``postCommitHandoff``, then (when live executing) **post-commit fleet batch** work runs under ``MissionRunCommandQueueTag/reserveSwapPostCommit`` with ``MissionRunReserveSwapPostCommitBatchAckContext`` so ``displacedMissionClear``, ``missionUpload``, and ``displacedFleetWindDown`` **pass/fail** lines are written **after** catalogue / recipe fleet acks (not merely at enqueue); the first awaited failure **short-circuits** remaining steps and posts an operator toast via ``GuardianReserveSwapPostCommitOperatorToastNotification`` (see ``MissionRunReserveSwapOperatorCopy`` post-commit strings). Operator toasts for pre-commit flows remain centralized in ``MissionRunReserveSwapOperatorCopy`` (`MissionControlModels.swift`).

**Reserve swap mission upload slice (v1)** — ``MissionRunReserveMissionUploadSliceKind`` / ``MissionRunReserveSwapMissionUploadPolicy`` (`MissionRunReserveSwapMissionUploadPolicy.swift`). **Default:** ``fullTaskMission`` (upload the full compiled task mission for the vacancy’s task — same mission envelope shape as normal MC task upload for that path). **Optional:** ``partialFromActiveExecutionCursor`` only after a validated ``MissionRunReserveMissionPartialCursor`` (hub ``missionProgressCurrent`` / ``missionProgressTotal``, optional ``taskCycleIndex``) satisfies ``MissionRunReserveSwapMissionUploadPolicy/validate``; partial tails still need planner / encoder work before dispatch. Align cursor capture with active MAVLink mission state **before** choosing partial.

**Reserve swap mission upload synchronisation (v1)** — ``MissionRunReserveMissionUploadActiveSynchronisationKind`` in the same file. **Default:** ``hardPauseActiveUntilUploadCompletes`` (hold active pattern / run pause until reserve upload finishes or swap aborts — exact pause vs task-hold is executor wiring). **Override:** ``concurrentActiveNoHold`` only when operators explicitly accept MAVLink mission list race risk for a faster handoff.

**Reserve swap mission upload verification (v1)** — ``MissionRunReserveMissionUploadVerificationKind`` + ``MissionRunReserveMissionUploadVerificationExpectation`` + ``MissionRunReserveSwapMissionUploadPolicy/validateVerificationReadiness`` in `MissionRunReserveSwapMissionUploadPolicy.swift`. **Default:** ``missionItemCountReadback`` (requires ``expectedMissionItemCount`` before scheduling the gate). **Alternates:** ``missionPlanContentHashMatch`` (non-empty ``expectedContentHash``) or ``recipeBasedMissionReadyProbe`` (valid ``missionReadyRecipeRaw``). Actual FC readback / recipe outcome checks are executor responsibilities; this API only rejects **impossible** gate configuration.

**Reserve swap mission upload failure (v1)** — ``MissionRunReserveMissionUploadFailureDispositionKind`` + ``MissionRunReserveMissionUploadDegradedHandoffAttestation`` + ``MissionRunReserveSwapMissionUploadPolicy/validateDegradedHandoffPrerequisites`` in the same file. **Default:** ``strictBlockUntilUploadAndVerificationSucceed`` (no roster commit / later phases on failed upload or verification). **Alternate:** ``allowDegradedHandoffAfterOperatorConfirmation`` only with a non-empty ``MissionRunReserveMissionUploadDegradedHandoffAttestation/acknowledgmentSummary`` (and matching ``missionRunID`` at emit sites). Automation must **not** take the degraded branch without an attestation.

**Reserve swap reposition geometry (v1)** — ``MissionRunReserveRepositionGeometryKind`` + ``MissionRunReserveRepositionFormationOffset`` + ``MissionRunReserveRepositionGeometryPolicy`` (`MissionRunReserveRepositionGeometryPolicy.swift`). Modes: **formation offset** (east/north metres from active hub), **rally to active hold**, **replace in place** when horizontal hub distance ≤ **``MissionRunReserveRepositionGeometryPolicy/colocatedHorizontalThresholdMeters``** (75 m v1). **Default kind:** ``rallyToActiveHoldPoint``. Use ``validateGeometryReadiness`` before issuing guided moves; positions come from **map + hub** telemetry (same sources as MC-R).

**Reserve swap reposition commands + RoE (v1)** — ``MissionRunReserveRepositionFleetVerb`` + ``MissionRunReserveRepositionCommandEngagementPolicy`` (`MissionRunReserveRepositionCommandEngagementPolicy.swift`) maps verbs to ``MissionRunEngagementAction`` (**RTL cancel** → ``rtl``; **guided goto / loiter** → ``swapInReserve`` umbrella). ``MissionRunEnvironment/repositionReserveFleetVerbEngagementGate(for:)`` returns ``MissionRunReserveRepositionEngagementGateOutcome`` from ``resolvedEngagementDisposition(for:)``: **autonomous** → immediate dispatch allowed; **forbidden** → block; **ask** / **defer** / **handoff** → require ``OperatorPromptOrigin/mreEngagementAsk`` or ``mreEngagementHandoff`` before dispatch (executor must wire prompts).

**Reserve swap reposition handoff proximity + timeout (v1)** — ``MissionRunReserveRepositionHandoffProximityPolicy`` (`MissionRunReserveRepositionHandoffProximityPolicy.swift`). **Defaults:** ``defaultHorizontalCloseEnoughMeters`` **40**, ``defaultVerticalCloseEnoughMeters`` **25**, ``defaultRepositionPhaseTimeoutSeconds`` **600** (10 min). Applies to **every** roster vehicle class: tolerances are magnitude defaults; the executor resolves hub pose vs anchor per class (aerial AGL vs UGV / surface semantics as telemetry allows). Use ``validateLockedDefaults()`` in tests / diagnostics.

**Reserve swap roster commit atomicity (v1)** — ``MissionRunReserveFloatingPoolRosterCommitStep`` + ``MissionRunReserveRosterCommitAtomicityPolicy`` (`MissionRunReserveRosterCommitAtomicityPolicy.swift`). **Single-call** floating swap: **pre-commit gate** → **exchange** roster vacancy binding with the chosen pool berth on the **same** pool row id → **log + derived state refresh** (see ``MissionRunEnvironment/swapRosterAssignmentWithRandomFloatingReserve`` / ``commitFloatingReservePoolPickToVacancy``). No pool append during swap-in.

**Reserve swap commit routing (v1)** — ``MissionRunReserveSwapCommitReserveSource`` + ``MissionRunReserveSwapCommitExecutionSurface`` + ``MissionRunReserveSwapCommitRoutingPolicy`` (`MissionRunReserveSwapCommitRoutingPolicy.swift`). **Pool berth** → ``swapRosterAssignmentWithRandomFloatingReserve`` (shipped). **Fixed template reserve row** → ``MissionRunEnvironment/swapRosterVacancyWithFixedTemplateReserveAssignment`` (shipped; ``commitReserveSwapInPending`` remains the umbrella name for a future unified ``commitReserveSwapIn(…)``). Callers should branch on ``executionSurface(for:)`` until the unified API lands.

**Reserve swap plan recompilation + plugin assistants (v1)** — ``MissionRunReserveSwapPlanRecompilationPolicy`` (`MissionRunReserveSwapPlanRecompilationPolicy.swift`). After roster/pool commit, ``MissionControlStore/recompileMissionControlPlanAfterFloatingReserveSwap`` must reach ``MissionRunPlannerSubsystem/compileInitialPlan`` with ``floatingReserveSwapPlanCompileSource`` (pool) or ``fixedRosterReserveSwapPlanCompileSource`` (fixed template reserve ↔ vacancy) so **planning** hooks (``MissionRunPlanningAssistant``) and **mutation commit** hooks (``MissionRunPlanningMutationAssistant`` `didApply`) run in **lexicographic registration key order** on the planner callback maps. Plugins install objects with ``MissionRunEnvironment/installAssistant(_:key:)``; conforming to ``MissionRunPlanningMutationAssistant`` registers both proposal vetting and **post-apply** notification — this is the supported way for assistant-owned rollups to track compiled plan + ``MissionControlPlanChangeResult`` after a reserve swap, not a Paladin-specific side channel. **Post-commit handoff (v1):** in the same MainActor turn after that recompile (when mission + fleet context allow recompile), ``MissionRunEnvironment/beginPostCommitReserveSwapHandoffPipeline`` resolves **vacancy** vs **displaced** fleet tokens, logs ``MissionRunReserveSwapPipelinePhase/postCommitHandoff``, and (when ``MissionRunReserveSwapMidCycleExecutionInvariantPolicy/isLiveExecutingSession`` and ``MissionRunEnvironment/effectiveExecutionContextForDispatch`` allow) enqueues **one** immediate ``MissionRunQueuedCommandBatch`` (tag ``MissionRunCommandQueueTag/reserveSwapPostCommit``) carrying ``MissionRunReserveSwapPostCommitBatchAckContext``, dispatched **sequentially** (same await sequencing as mission-clear + recipe batches in ``MissionRunExecutionSubsystem``) so clears and recipes do not race: catalogue ``fleetVehicleDoMissionClear`` on the **displaced** row when its token parses, **then** ``MissionRunFleetDispatch/recipe`` on the **vacancy** row using ``recipe.fleet.do.mission.upload.start`` (upload → arm → start) or ``recipe.fleet.do.mission.upload.start.item`` when displaced hub ``missionProgressCurrent`` is above zero (upload → ``fleetVehicleDoMissionJumpTo`` with ``missionStartItemIndex`` → arm → start), **then** the first dispatchable step from ``MissionRunPolicyResolution/resolvedReserveSwapPreferenceChain`` on the **displaced** row (RTL / nearest-open move+park / loiter / park — same optimistic shapes as ``MissionRunExecutionSubsystem/buildCompletePolicyWindDownCommands``) via ``MissionRunExecutionSubsystem/buildReserveSwapPolicyWindDownCommands``. **There is no separate post-commit “mission start only” command** in v1 — arm and start are recipe steps on the vacancy. Squad JSON is built from the compiled task mission for the vacancy’s task (``MissionRunPlannerSubsystem/buildTaskSquadMissions`` + ``reserveSwapPlannedSquadForVacancy``). **Phase logs** for ``displacedMissionClear``, ``missionUpload``, and ``displacedFleetWindDown`` in this batch reflect **fleet ack outcomes** (see **Reserve swap phase logging**); upload verification readback and partial-tail uploads remain policy work in ``MissionRosterReservesToDo.md``.

**Reserve swap mid-cycle execution invariants (v1)** — ``MissionRunReserveSwapMidCycleExecutionInvariantPolicy`` (`MissionRunReserveSwapMidCycleExecutionInvariantPolicy.swift`). When ``status`` is ``running`` / ``paused`` / ``recovery`` and ``sessionPhase`` is ``executing``, a floating-pool roster commit is **mid-cycle**: ``taskCyclesCompletedByTaskID``, ``cyclesCompleted``, ``activeCycleTaskIDs``, and the executor **command queue** are **not** reset by ``swapRosterAssignmentWithRandomFloatingReserve`` (only ``refreshDerivedTaskStates`` + caller-driven plan recompile). **Queue coherence:** each ``MissionRunIssuedCommand`` carries a snapshot ``vehicleTokenKey``; after swap, the same ``assignmentID`` may bind a new token, so pending ``MissionRunQueuedCommandBatch`` rows can be **stale** relative to roster — use ``pendingBatchesContainStaleVehicleTokenForAssignment`` to detect. v1 **does not** ship selective per-command cancellation APIs on the queue (only tag + dispatch-shape filtering); live swap-in executors must reconcile or rebuild affected batches explicitly.

**Reserve swap disposition of replaced active (v1)** — `MissionRunReserveSwapReplacedActiveDispositionPolicy.swift` (pool: ``MissionRunReserveSwapReplacedActivePoolDispositionPolicy``; fleet wind-down: ``MissionRunReserveSwapReplacedActiveFleetWindDownPolicy``; MC-R / map: ``MissionRunReserveSwapReplacedActiveTelemetryHandoffPolicy``). **Policy resolution:** ordered **reserve swap** tactics for displaced-active wind-down use the same **assignment → task → mission** precedence as abort/complete (``MissionRunPolicyResolution/resolvedReserveSwapPreferenceChain``); mission default is ``RouteRules/missionReserveSwapPreferenceChain`` (``MissionRunReserveSwapTactic``). **Binding destination (swap-in commit):** ``MissionRunReserveSwapReplacedActiveReturnPathPolicy`` — floating pool swap-in writes the displaced roster binding onto **the same pool berth id** that supplied the reserve (in-place exchange; not ``returnAssignmentToReservePool``); fixed template reserve swap-in **exchanges** tokens between the vacancy row and the **source `.reserve` roster assignment** (pool unchanged). **Pool / written-off:** a successful floating swap still requires the vacating roster binding to be **eligible** to occupy the pool berth (same gates as ``returnAssignmentToReservePool`` for fleet rows); rejections fail the swap atomically — use ``markFleetVehicleWrittenOffForReservePool`` and operator alternate paths when a **vehicle** cannot sit in that pool row. **Fleet:** RTL / rally (nearest open mission point) / loiter / park uses ``MissionRunFleetDispatch/preferentialReserveSwapTacticDispatch`` (and the same catalogue + recipe atoms as complete planning); orderly swap-out on the post-commit row that still owns the replaced token should resolve ``MissionRunPolicyResolution/resolvedReserveSwapPreferenceChain`` before issuing wind-down; abort-class stop uses ``MissionRunPlannerSubsystem/buildAbortPlan`` semantics. **Telemetry handoff:** running UI foregrounds the **vacancy** (new active); the replaced **vehicle** stays on the fleet map until wind-down completes or link is lost.

**MCR (running):** Task triage header toggles the left roster strip between **task roster** health cards and **this task’s floating reserve berths** (browse/manage). Tapping a berth opens the **reserve pool berth** overlay (above roster vehicle overlay, below map points): vehicle status, choose/change vehicle via drawer (same pattern as MCS pool), **remove vehicle from berth** (clear binding for replacement), **arm preflight** (audit ``missionControl.preflightProbe.reservePoolBerth``), and **Vehicle Inspector**. **Reserve swap-in (pool picker):** from a roster slot’s vehicle overlay, **Swap in reserve** switches the **left-column roster health-card strip** to enumerated floating-pool candidates for that vacancy (same order as ``enumerateReserveSwapCandidates``); when **no** pool row can replace this vacancy, ``MissionRunEnvironment/floatingReservePoolPickStripEmptyOperatorCopy`` supplies differentiated operator copy (no berths, no bindings, written-off pool, class mismatch, duplicate fleet token vs vacancy, or operational draw failure) and may append a **template reserve row** hint when a fixed ``.reserve`` roster candidate still exists on the task. Each pool row shows a neutral **class** capsule (``FleetVehicleType/classCode``) and a **battery** icon with percent when ``FleetVehicleOperationalModel`` has usable data (``MissionLiveVehicleHealthCard`` / ``reservePoolPickerChrome``). Tap a pool card, then confirm: **swap-in snapshot gates** (staleness, in-air inconsistency while disarmed, ``health.armable`` / ``health.global_position``, GPS fix short via hub → ``FleetVehicleOperationalModel``, battery **critical** band, blocked flight-mode substrings) run first with **no** ``FleetRecipeRunner`` audit; then the **arm probe** recipe runs (``MissionControlStore/runSingleVehiclePreflightProbe`` with ``MissionControlPreflightTelemetryGateMode/reserveSwapIn``, ``allowDuringLiveMission: true``, audit ``missionControl.preflightProbe.reserveSwapIn``); on success ``MissionRunEnvironment/swapRosterAssignmentWithFloatingReservePoolSlot`` commits. If any check fails, a **bottom prompt** (``GuardianBottomPromptCenter/presentTripleChoice``) offers **Cancel** (leave the swap), **Switch** (dismiss the prompt and pick another pool row), or **Inspect** (Vehicle Inspector on that reserve); tap the same pool row again to retry. **Cancel reserve swap** restores the normal strip. Eligible pool markers on the live map use ``MapVehicleMarker/selected`` and a subtle **pulse** (``MapVehicleMarker/selectionAttentionPulse`` → Leaflet CSS in ``OSMMapView``) while picking. **Live overview map:** fleet-bound pool slots with hub position render as vehicle markers (label `… · pool`); ids use ``MissionControlReservePoolMapMarkerID`` (`MissionControlReservePoolMapMarkerID.swift`); tap opens that task’s triage; **Show on map** from triage includes pool positions in fit bounds. **VoiceOver:** ``MissionRunReserveSwapAccessibilityCopy`` supplies ``MapVehicleMarker/accessibilityTitle`` (Leaflet marker `title` and thumbnail ``alt``) for pool markers and for roster **vacancy** / **bench reserve** markers while a swap pick targets that task, and single-element ``MissionLiveVehicleHealthCard`` labels for swap/browse pool strips and empty states.

**Tests:** `MissionRunReservePoolTests`, `MissionRunPlannerReservePoolMutationTests`, `MissionControlReservePoolMapMarkerIDTests`, `MCSReservePoolHomeStagingMapEligibilityTests`, `MCSReservePoolHomePlacementTemplateGuardTests`, `McsReservePoolHomeMapBatchLogTemplateCatalogTests`, `FleetVehicleTypeSubstitutionTests`, `MissionRunReserveSwapCandidateEnumerationTests`, `ReserveSwapCandidateAliasTests`, `MissionRunReserveAutoSuggestPolicyTests`, `MissionRunReserveAutoSwapExecutorPolicyTests`, `MissionRunReserveAutoSwapLiveEvaluatorTests`, `MissionRunReserveSwapRankingPolicyTests`, `MissionRunReserveSwapTimeChecklistTests`, `MissionRunReserveSwapFailureBranchPolicyTests`, `MissionRunReserveSwapPostCommitHandoffPolicyTests`, `MissionRunReserveSwapDisplacedMissionClearTests`, `MissionRunReserveSwapPostCommitWindDownTests`, `MissionRunReserveSwapPostCommitVacancyMissionRecipeSelectionTests`, `MissionRunReserveSwapPostCommitPipelinePhaseResolverTests`, `MissionRunReserveRecipeRunnerCorrelationTests`, `MissionRunReserveSwapPhaseLogTemplateKeyTests`, `MissionRunReserveSwapPhaseLogEmissionTests`, `MissionRunReserveSwapMissionUploadPolicyTests`, `MissionRunReserveRepositionGeometryPolicyTests`, `MissionRunReserveRepositionCommandEngagementPolicyTests`, `MissionRunReserveRepositionHandoffProximityPolicyTests`, `MissionRunReserveRosterCommitAtomicityPolicyTests`, `MissionRunReserveSwapCommitRoutingPolicyTests`, `MissionRunReserveSwapPlanRecompilationPolicyTests`, `MissionRunReserveSwapMidCycleExecutionInvariantPolicyTests`, `MissionRunReserveSwapReplacedActiveDispositionPolicyTests`, `MissionRunReserveSwapReplacedActiveReturnPathPolicyTests`, `MissionRunReserveSwapCommitSourcesFleetParityTests`, `MissionRunFloatingReservePoolPickStripEmptyCopyTests`, `MissionRunFloatingReserveTargetedPoolSwapTests`, `MissionRunOrderedStartRunPreflightProbeTargetsTests`, `MissionRunFixedRosterReserveSwapTests`, `MissionControlReserveSwapInPreflightGatesTests`, `MissionControlStoreFloatingReserveSwapRecompileTests`, `MissionRunReserveSwapOperatorCopyTests`, `MissionRunPaladinReserveSwapProposalPolicyTests`, `MissionRunReserveSwapAccessibilityCopyTests`.

**Reserve swap verification —** **Unit / non-SITL:** the suites above run in-process with synthetic ``MissionRunEnvironment`` values and hub snapshots (including ``MissionControlReserveSwapInPreflightGatesTests``); they do **not** require a live MAVSDK or SITL session. **SITL:** extending ``GuardianHQSitlSmokeTests`` with a per-stack happy-path reserve swap remains **optional** until the live swap executor is stable enough for CI.

**Deferred phases** (auto triggers, richer audit, operator written-off flows, executor draws while running, optional full pool edit in MC-R): see **`NEXTVERSION.md`** → **Floating reserve pool — deferred phases** (2026-05-12).

**Live reserve swap-in** (replace active with reserve: arm, mission handoff, reposition, roster commit, disposition of replaced **vehicle**, class matching, escalation / manual / auto triggers; **pool + fixed `.reserve`**): **`MissionRosterReservesToDo.md`**.

## Mission run state model (run vs per-task)

Authoritative derivation: ``MissionRunEnvironment/refreshDerivedTaskStates()`` → ``deriveMissionTaskState(task:run:now:)`` and ``deriveMissionTaskAttemptState(task:run:)`` in `MissionRunEnvironment.swift`. Types: `MissionRunStatus`, `MissionRunSessionPhase`, `MissionTaskState`, `MissionTaskAttemptState` in `MissionControlModels.swift`.

**Run layer (`MissionRunStatus` × `MissionRunSessionPhase`).** `MissionRunLifecycleSubsystem` moves **status**; ``MissionRunEnvironment/setSessionPhase`` updates **sessionPhase** and refreshes task states. ``MissionRunExecutionSubsystem/completeRun`` is the main bridge from **whole-run** stop/complete into session phase: **abort protocol** sets `sessionPhase` to ``MissionRunSessionPhase/aborting`` without flipping ``MissionRunStatus`` away from ``MissionRunStatus/running`` or ``MissionRunStatus/paused``; **recovery** sets ``MissionRunStatus/recovery`` and ``MissionRunSessionPhase/recovery``. ``MissionRunEnvironment/beginRun()`` sets ``MissionRunStatus/running`` + ``MissionRunSessionPhase/staging``; ``startExecution`` calls ``markExecuting()`` → ``MissionRunSessionPhase/executing``. **Pause / resume** only toggles ``MissionRunStatus`` (``paused`` ↔ ``running``); session phase is unchanged. **Lifecycle:** ``markCompleted`` → ``MissionRunStatus/completed`` + ``MissionRunSessionPhase/completed``; ``markFailed`` → ``MissionRunStatus/completed`` + ``MissionRunSessionPhase/aborted``; ``resetToSetup`` → ``MissionRunStatus/setup`` + ``MissionRunSessionPhase/draft``.

**Per-task layer (`MissionTaskState`).** Derived map: ``MissionRunEnvironment/taskStateByTaskID``. Special cases: a **disabled** task is always ``MissionTaskState/ready``. If ``operatorTriageMarkedMissionTaskStateByTaskID`` pins ``MissionTaskState/aborted`` or ``MissionTaskState/completed``, derivation returns that pin verbatim (manual triage wins). **Whole-run aborting session:** while ``MissionRunStatus`` is still ``running`` or ``paused`` and ``sessionPhase`` is ``aborting`` or ``aborted``, each enabled task is ``MissionTaskState/aborted`` iff its id is in ``taskMissionEndAbortCompletedByTaskID``, else ``MissionTaskState/aborting``. **Whole-run completed status:** if ``sessionPhase`` is ``MissionRunSessionPhase/aborted`` (e.g. ``markFailed``), the same abort-ack rule applies; otherwise every enabled task reads ``MissionTaskState/completed``. **Whole-run recovery status:** ``MissionTaskState/completed`` when the task id is in ``taskMissionEndRecoveryCompletedByTaskID``, else ``MissionTaskState/recovery``. **Setup:** ``MissionTaskState/ready`` for most session phases; ``MissionTaskState/staging`` only when ``sessionPhase`` is ``staging``.

**While ``MissionRunStatus`` is ``running`` or ``paused`` and ``sessionPhase`` is ``executing``** (the usual MC-R path): **task-scoped** abort/complete wind-down uses ``missionTaskAbortWindDownIssuedTaskIDs`` / ``missionTaskCompleteWindDownIssuedTaskIDs`` (set when dispatch actually runs — see ``MissionRunExecutionSubsystem/performImmediateMissionTaskAbort``, ``performImmediateMissionTaskComplete``, and end-of-cycle delivery in ``deliverPendingMissionTaskGracefulWindDownsIfNeeded``). For a task with abort wind-down issued, state is ``MissionTaskState/aborted`` iff ``taskMissionEndAbortCompletedByTaskID`` contains the task; else ``MissionTaskState/aborting``. For complete wind-down issued, ``MissionTaskState/completed`` iff ``taskMissionEndRecoveryCompletedByTaskID`` contains the task; else ``MissionTaskState/recovery``. **Deferral:** if ``taskStartDeferralByTaskID`` is active (``now < startAt``), state is ``MissionTaskState/staging``. **Cycles:** ``activeCycleTaskIDs`` → ``MissionTaskState/executing``; finite continuous / continuous-with-delay with all cycles done → recovery vs completed via recovery ack set; repeating with at least one cycle done → ``MissionTaskState/between``; else ``MissionTaskState/ready``.

**Acknowledgement / triage APIs** (today UI-driven; future automation should call the same entry points): ``operatorMarkMissionTaskTriageState(taskID:state:)`` (``.aborted`` / ``.completed`` only), ``acknowledgeTaskMissionEndRecovery(taskID:)``, ``acknowledgeTaskMissionEndAbort(taskID:)``. Abort triage also invokes ``promoteSessionPhaseToAbortedIfAllTasksAcknowledgedAbort()`` when every **enabled** task id is in ``taskMissionEndAbortCompletedByTaskID`` while ``sessionPhase == .aborting`` — then session moves to ``MissionRunSessionPhase/aborted`` without ending the run.

### Task attempting vs current (`MissionTaskAttemptState`)

**Settled label:** ``MissionTaskState`` in ``taskStateByTaskID`` (above). **In-flight intent:** ``MissionTaskAttemptState`` in ``MissionRunEnvironment/taskAttemptingByTaskID`` — recomputed in the same ``refreshDerivedTaskStates()`` pass as ``taskStateByTaskID``.

**v1 derivation (orchestration flags only):** for each **enabled** task, if operator triage pins ``MissionTaskState/aborted`` or ``MissionTaskState/completed``, **no** attempting row (intent is folded into triage). Else, **precedence:** (1) ``missionTaskAbortWindDownIssuedTaskIDs`` → ``MissionTaskAttemptState/abortWindDownIssued``; (2) else ``missionTaskCompleteWindDownIssuedTaskIDs`` → ``MissionTaskAttemptState/recoveryWindDownIssued``; (3) else ``pendingMissionTaskGracefulWindDownKindByTaskID`` ``abortAfterCycle`` / ``completeAfterCycle`` → ``abortWindDownScheduledAfterCycle`` / ``recoveryWindDownScheduledAfterCycle``. **Disabled** tasks omit attempting entries even if internal maps were touched out-of-band.

**Future (slot evidence):** when per-assignment state + push/pull evidence land (``TaskRosterAssignmentStatesToDo.md`` §3+), attempting may clear **before** triage when all bound slots satisfy policy — without changing ``MissionTaskState`` until rollup rules say so.

### Task derivation — inputs, refresh triggers, ack-set writers

**What `deriveMissionTaskState` reads (no hidden globals):** the current ``Mission`` template (``MissionTask/enabled``, ``regularity``, ``cycles``), ``MissionRunEnvironment/status``, ``sessionPhase``, ``operatorTriageMarkedMissionTaskStateByTaskID``, ``taskMissionEndAbortCompletedByTaskID``, ``taskMissionEndRecoveryCompletedByTaskID``, ``missionTaskAbortWindDownIssuedTaskIDs``, ``missionTaskCompleteWindDownIssuedTaskIDs``, ``taskStartDeferralByTaskID`` (vs ``now``), ``activeCycleTaskIDs``, and ``taskCyclesCompletedByTaskID``. Wind-down **issued** flags come from executor dispatch; **pending after-cycle** intent lives in ``pendingMissionTaskGracefulWindDownKindByTaskID`` (consumed at cycle boundary — see **Task-scoped wind-down flags** below).

**Who mutates the terminal ack sets:** ``taskMissionEndAbortCompletedByTaskID`` and ``taskMissionEndRecoveryCompletedByTaskID`` gain/lose membership **only** inside ``operatorMarkMissionTaskTriageState`` (including ``acknowledgeTaskMissionEndAbort`` / ``acknowledgeTaskMissionEndRecovery``, which delegate there). **Per-task reset:** ``prepareMissionTaskForOperatorRestart`` clears triage pin, both ack sets’ entries for that task, wind-down issued flags, and pending graceful map for that task. **Whole-run reset:** ``MissionRunLifecycleSubsystem/resetToSetup`` calls ``clearTaskMissionEndRecoveryAcknowledgements`` and ``clearTaskMissionEndAbortAcknowledgements`` (empty both sets). There is **no** automatic insertion from fleet telemetry or recipe outcomes today — that is the gap **TaskRosterAssignmentStatesToDo.md** §3 / §4 tracks.

**Who mutates the operator triage pin map:** same ``operatorMarkMissionTaskTriageState`` for writes; ``prepareMissionTaskForOperatorRestart`` removes one task; ``clearMissionTaskScopedOrchestrationState`` wipes the whole map (also used from ``beginRun``, execution start, and ``completeRun`` teardown paths).

**When `refreshDerivedTaskStates` runs:** always after ``setSessionPhase``; on ``MissionRunEnvironment`` init and convenience init; ``updateTemplate``; floating-reserve swap success; task-scoped graceful pending set/clear/consume; abort/complete wind-down **issued** markers; ``operatorMarkMissionTaskTriageState``; ``prepareMissionTaskForOperatorRestart``; deferral mutations; ``clearActiveCycleTasks`` / ``markTaskActiveInCurrentCycle``; ``recordTaskCycleCompletions``; ``clearTaskStartDeferral``; **store / UI:** ``MissionControlStore`` (create run, update run, start run, compile plan, reset-to-setup partial), ``MissionControlSetupView`` (one diagnostic refresh), ``MissionRunLifecycleSubsystem`` pause / resume / reset-to-setup. The same pass recomputes ``taskAttemptingByTaskID`` (see **Task attempting vs current** above).

**Graceful per-task intent (next cycle)** is detailed under **Task-scoped wind-down flags** below (pending map vs whole-run ``gracefulStopKind``).

### Task-scoped wind-down flags (pending map + issued sets)

These three fields on ``MissionRunEnvironment`` drive MC-R “abort/complete **in progress**” vs “scheduled for **end of this autopilot cycle**” for a **single task**. They are **orthogonal** to whole-run ``MissionRunGracefulStopKind`` (after-cycle **entire run** stop), which uses queued command batches instead of this per-task map.

| Field | Role | When it is **set** | When it is **cleared** |
| --- | --- | --- | --- |
| ``pendingMissionTaskGracefulWindDownKindByTaskID`` | Operator (or API) asked **abort after cycle** or **complete after cycle** for task `T`; entry removed when the cycle boundary **consumes** it and dispatch runs. | ``MissionRunSchedulingSubsystem/abortMissionTaskAfterCycle`` / ``completeMissionTaskAfterCycle`` → ``setPendingMissionTaskGracefulWindDown`` (after ``clearPendingMissionTaskGracefulWindDown(forTaskID:)`` for that task). Preconditions: ``gracefulStopKind == .none``, task **enabled**, at least one ``assignmentsBoundToMissionTask`` row; otherwise a log line and **no** map entry. | ``consumePendingMissionTaskGracefulWindDown(forTaskID:)`` (executor at cycle end); ``clearPendingMissionTaskGracefulWindDown`` for one task or for all; ``MissionRunSchedulingSubsystem/revokeMissionTaskGracefulWindDown``; ``revokeGracefulAfterCycleStop`` (whole-run revoke → clears **all** pending task entries); ``clearMissionTaskScopedOrchestrationState``; ``prepareMissionTaskForOperatorRestart`` (clears that task’s pending first). |
| ``missionTaskAbortWindDownIssuedTaskIDs`` | Abort-policy fleet wind-down for task `T` has **actually been dispatched** (immediate or post–cycle-boundary delivery). | ``markMissionTaskAbortWindDownIssued(forTaskID:)`` from ``MissionRunExecutionSubsystem`` after command dispatch for that task (immediate path, async mission-clear barrier path, or ``deliverPendingMissionTaskGracefulWindDownsIfNeeded``). **Also** inserts ``taskID`` into ``missionTaskAutopilotAutostartSuppressedTaskIDs`` in the same call. | ``clearMissionTaskScopedOrchestrationState``; ``operatorMarkMissionTaskTriageState`` when recording **aborted** or **completed** triage (removes that task from issued sets as part of normalising state); ``prepareMissionTaskForOperatorRestart``. |
| ``missionTaskCompleteWindDownIssuedTaskIDs`` | Complete-policy recovery wind-down for task `T` was **dispatched**. | ``markMissionTaskCompleteWindDownIssued(forTaskID:)`` from the executor in the same three delivery shapes as abort. **Also** inserts into ``missionTaskAutopilotAutostartSuppressedTaskIDs``. | Same three clear paths as abort issued. |
| ``missionRunAssignmentIDsWithOperatorLiveDriveHandoff`` | MC‑R **Engage Live Drive** marked this roster / pool **assignment** id: block autonomous reserve distress automation on that vacancy and merge mapped mission tasks into the executor’s next-cycle MAVLink **autostart** suppression union (with ``missionTaskAutopilotAutostartSuppressedTaskIDs`` — wind-down semantics stay on that set alone). | ``noteOperatorLiveDriveHandoffActive(forAssignmentID:)`` from ``MissionControlSetupView`` after stabilize + pending batch cancel, before drill-in to Live Drive. | ``clearOperatorLiveDriveHandoffs(matchingResolvedFleetVehicleID:fleetLink:sitl:)`` via ``MissionControlStore/clearOperatorLiveDriveHandoffForClearedControlSessionVehicle`` when ``FleetLinkService/clearLiveDriveControlSessionVehicleIfMatches`` runs (``LiveDriveView`` session end / stream setup failure, ``VehiclesView`` teardown); per-row ``clearOperatorLiveDriveHandoff(forAssignmentID:)``; ``clearMissionTaskScopedOrchestrationState``. |

**Operator revoke (MC Setup):** ``MissionRunEnvironment/revokeMissionTaskGracefulWindDown(forTaskID:)`` forwards to scheduling → ``clearPendingMissionTaskGracefulWindDown`` only; it does **not** clear issued sets (those mean “commands already went”).

**UI read model:** ``MissionControlSetupView`` reads pending vs issued for per-task banners/chips (same three fields). For intent vs settled labels, also read ``MissionRunEnvironment/taskAttemptingByTaskID`` (``MissionTaskAttemptState``).

**Auto-completion wiring:** any future “fleet said we’re done” path must either call the existing triage/ack APIs or update **new** slot-level state first; it must **not** silently clear ``missionTask*WindDownIssued`` without matching product rules, or autopilot autostart suppression can desynchronise from reality.

### MissionRunSessionPhase (enum + transitions + abort promotion)

**Enum** (`MissionControlModels.swift`): ``draft`` (default new run) → ``compiled`` (plan compiled in MCS) → ``staging`` (countdown / one-off schedule / post-``beginRun`` upload pass) → ``executing`` (live mission cycles) → ``recovery`` (whole-run success wind-down; paired with ``MissionRunStatus/recovery``) or ``aborting`` / ``aborted`` (abort protocol while run still ``running``/``paused``) → ``completed`` (paired with ``MissionRunStatus/completed`` after operator marks run done). Comments on the enum are authoritative for operator meaning.

**Who calls ``MissionRunEnvironment/setSessionPhase`` (only mutator):**

| New phase | Typical caller |
| --- | --- |
| ``draft`` | ``MissionRunLifecycleSubsystem/resetToSetup`` |
| ``compiled`` | ``MissionRunLifecycleSubsystem/markCompiled`` |
| ``staging`` | ``beginRun``; ``MissionRunSchedulingSubsystem/scheduleDeferredOneOffExecution`` (one-off future start — may occur while ``MissionRunStatus`` is still ``setup``); **not** cleared automatically when status flips to ``running`` |
| ``executing`` | ``MissionRunLifecycleSubsystem/markExecuting`` (``startExecution``) |
| ``recovery`` | ``MissionRunExecutionSubsystem/completeRun(… operatorWindDown: .recoveryPhase)`` |
| ``aborting`` | ``completeRun(… operatorWindDown: .abortProtocolPhase)`` (whole-run abort paths) |
| ``aborted`` | ``MissionRunLifecycleSubsystem/markFailed`` (run **status** becomes ``completed`` here — terminal failure); **or** ``promoteSessionPhaseToAbortedIfAllTasksAcknowledgedAbort`` (see below) |
| ``completed`` | ``MissionRunLifecycleSubsystem/markCompleted`` |

**Pause / resume** does not change session phase. **Task-scoped** abort/complete does not move session phase by itself (run stays ``executing`` until a whole-run ``completeRun`` / lifecycle call above).

**``promoteSessionPhaseToAbortedIfAllTasksAcknowledgedAbort``** (private on ``MissionRunEnvironment``; invoked from ``operatorMarkMissionTaskTriageState`` after an **aborted** triage write): promotes ``aborting`` → ``aborted`` only when (1) ``MissionRunStatus`` is ``running`` **or** ``paused``, (2) current phase is ``aborting``, (3) every **enabled** ``MissionTask`` id in the template is present in ``taskMissionEndAbortCompletedByTaskID``. **Disabled** tasks are excluded from that denominator — they do not require an abort acknowledgement row. There is **no** per-roster-assignment gate today; future **per-slot** abort evidence would extend or replace the subset check on ``taskMissionEndAbortCompletedByTaskID`` (or introduce a parallel slot-ack set) rather than overloading task ids.

### MissionRunAssignment (roster row model)

One **roster row** in MCS / MRE is a ``MissionRunAssignment`` (`MissionControlModels.swift`). Rows live in ``MissionRunEnvironment/assignments`` (built when ``MissionControlStore/createRun(from:cloningMissionRunDefaultsFrom:)`` walks each task’s ``rosterDeviceIds``, or one row per ``Mission/rosterDevices`` when no task bindings exist). The type is **Codable** for persistence / handoff. **Slot lifecycle (v2):** optional ``slotLifecycleLanes`` (``MissionRunAssignmentSlotStateLanes``); absent in older JSON — use ``effectiveSlotLifecycleLanes`` (defaults both lanes to ``idle`` until writers populate the field). **Persisted slot state is locked to option (a) on-row** — see **Roster slot state storage** below.

| Field | Role |
| --- | --- |
| ``id`` | Stable row identity (commands, plan role tracks, logs). |
| ``taskId`` | Owning ``MissionTask`` id when known; **nil** for legacy “unscoped” rows. |
| ``rosterDeviceId`` | Join key into ``Mission/rosterDevices`` (template slot). |
| ``slotName`` | Operator label (usually device name at creation). |
| ``attachedFleetVehicleToken`` | Parsed ``FleetMissionVehicleToken/storageKey`` when the row is bound to the fleet list; **nil** when empty. |
| ``attachedDevice`` | Legacy / free-text binding when not using fleet tokens. |
| ``policies`` | ``MissionRunAssignmentPolicies``: optional **non-empty** ``abortPreferenceChain`` / ``completePreferenceChain`` / ``reserveSwapPreferenceChain`` overrides (otherwise task → mission defaults apply via ``MissionRunPolicyResolution``). |
| ``slotLifecycleLanes`` | Optional **commanded** / **observed** slot states; **nil** in legacy payloads and for rows that never persisted lanes. |

**Policy resolution:** ``MissionRunPolicyResolution/resolvedTaskId(for:mission:)`` returns explicit ``taskId``, else the sole **enabled** task’s id when exactly one enabled task exists, else **nil** (ambiguous). Abort, complete, and reserve-swap chains use **assignment → task → mission** (first non-empty wins, then normalised).

**“Bound to task” for dispatch:** ``MissionRunEnvironment/assignmentsBoundToMissionTask(taskID:)`` includes rows whose ``taskId`` matches **or** whose ``id`` appears on the compiled plan’s ``roleTracks`` with that ``taskID`` (covers planner-reassigned topology without rewriting ``taskId`` on the row).

**Floating reserve probes:** ``MissionRunAssignment/syntheticForReservePool`` builds a throwaway row (``taskId`` nil; ``rosterDeviceId`` / ``id`` tied to the pool slot) for fleet resolution and Mission Preflight — not a persisted MCS roster edit.

**Codable notes:** decode accepts legacy JSON key ``pathId`` as ``taskId``; ``policies`` defaults to empty; encode omits ``policies`` when both override chains are absent or empty (smaller payloads). **Slot lanes:** ``slotLifecycleLanes`` is ``decodeIfPresent`` — older assignment JSON without the key loads as ``nil``; ``encodeIfPresent`` omits the key when unset. Persisted slot evidence stays on the row only (option **(a)** below); do not add a parallel ``assignmentID → lanes`` map on the run envelope without revisiting this lock.

### Roster slot state storage (design tradeoffs)

**Locked (2026-05-11): option (a) on-row.** Per-**assignment** slot lifecycle state lives on ``MissionRunAssignment`` (today: optional ``slotLifecycleLanes`` + ``effectiveSlotLifecycleLanes``). **Rationale:** it already ships in Codable run payloads; SwiftUI and persistence naturally follow ``assignments`` / stable ``MissionRunAssignment/id``. **(b)** parallel map and **(c)** hybrid are **not** in use — do not introduce a second persisted source of truth for the same slot without an explicit product revision and README update.

| Option | Shape | Status |
| --- | --- | --- |
| **(a) On-row** | Field(s) on ``MissionRunAssignment`` (``slotLifecycleLanes`` today) | **Locked** — extend on this type when §3–§4 writers land. |
| **(b) Parallel map** | ``[UUID: …]`` on ``MissionRunEnvironment`` keyed by ``assignment.id`` | **Not chosen** — drift risk vs persisted ``assignments``. |
| **(c) Hybrid** | Row markers + ephemeral overlay | **Not chosen** — two layers deferred unless product reopens the decision. |

**Synthetic / pool rows:** ``MissionRunAssignment/syntheticForReservePool`` is an **in-memory** probe helper (preflight / fleet resolution). It does **not** add ``slotLifecycleLanes``; ``effectiveSlotLifecycleLanes`` reads as idle until a real persisted roster row gains writers. Floating-reserve **pool** berths are **binding inventory** on ``MissionRunReservePoolSlot`` / ``reservePoolByTaskID`` (see **Floating reserve pool**); orchestration **slot lifecycle** for policy/evidence keys off the roster row’s ``assignment.id`` once the aircraft is committed to ``assignments`` (including after swap-in).

**Cross-cutting concerns:** ``assignmentsBoundToMissionTask`` / compiled ``roleTracks`` can move which row counts for a task without changing ``taskId`` — slot state must follow **assignment id**, not template slot name alone. Pool berth identity vs roster ``assignment.id`` is documented in **Floating reserve pool** and ``MissionRosterReservesToDo.md`` for swap-in handoff.

**Roster slot state enum (v1):** ``MissionRunAssignmentSlotState`` in `MissionControlModels.swift` — ``idle``, ``staging``, ``executingMission``, ``betweenCycles``, ``policyAborting``, ``policyCompleting``, ``policySucceeded``, ``policyFailed``, ``blockedNoVehicle``, ``notApplicableEmptySlot``, ``supersededReassigned``. Operator-facing chip strings are ``MissionRunAssignmentSlotState/displayTitle`` (**v1 UX lock**); extend or refine when §3 evidence catalogue lands. Avoid autopilot mode names as stand-ins for policy (e.g. do not label abort policy as “Loitering” unless the UI explicitly ties copy to FC mode).

**Commanded vs observed (v2):** ``MissionRunAssignmentSlotStateLanes`` holds **commanded** (MRE dispatch / policy issuance only) and **observed** (hub / recipe / pull conformance). **Writers:** only orchestration paths mutate ``commanded``; telemetry subscribers mutate ``observed`` — never copy hub ticks into ``commanded``. For a single chip label, ``MissionRunAssignmentSlotLaneMerge/preferredDisplayState(lanes:)`` implements the merge rules so **stale telemetry does not overwrite** an in-flight ``policyAborting`` / ``policyCompleting`` (or commanded terminal rows), while still surfacing ``policyFailed`` / ``blockedNoVehicle`` from evidence when commanded is still in an exploratory execution phase. Refine with the §3 evidence catalogue.

**MC-R / MCS roster slot chip (v1 UI contract):** While ``MissionRunStatus`` is ``running``, ``paused``, or ``recovery``, MCS roster accordion rows (``MissionControlRosterSlotCard``), MC-R live console tiles (``MissionLiveVehicleHealthCard``), and MC-R task list rows show a compact **slot** pill when ``MissionRunAssignmentSlotState/missionControlRosterBadgeSeverity`` is non-nil (``MissionControlModels.swift`` — slot state extensions). **Quiet states** (``idle``, ``notApplicableEmptySlot``) omit the pill. **Informational** paths use ``GuardianFeedbackSeverity/info`` (staging, on-mission, between cycles, recovery-in-progress, reassigned). **Abort in progress** uses ``warning``. **Policy complete** uses ``success``. **Policy failed** / **no vehicle bound** use ``error``. Capsule chrome uses ``GuardianSemanticColors`` + ``GuardianFeedbackSeverity/legacyTranslucentChipBackground`` (``MissionControlRosterSlotAttentionCapsule``). Task rows show the **worst** severity across that task’s roster assignments (``MissionControlAssignmentSlotRosterAttention/worstAmong``).

### Fleet evidence sources (Mission Control inventory)

Candidate signals for future **push** / **pull** slot auto-triage (``TaskRosterAssignmentStatesToDo.md`` §3); **today** none of these replace operator triage on ``MissionRunEnvironment``.

| Surface | What it captures | Roster correlation |
| --- | --- | --- |
| ``MissionRunIssuedCommand`` | Each dispatch carries ``assignmentID``, ``slotName``, ``vehicleTokenKey``, ``dispatch`` (raw vehicle command vs **catalogue** invoke vs **recipe** run), plus ``fleetDispatchSourceLabel`` (`issuer`:`issuerKey`) for ``FleetLinkService`` **source** strings. | Join to ``MissionRunAssignment/id``; resolve task via ``MissionRunPolicyResolution`` / compiled plan as elsewhere. |
| ``MissionRunEvent`` + structured logs | ``MissionRunCommandSubsystem/dispatchCommand`` appends dispatch / token / vehicle-unavailable events; catalogue and recipe paths append **fleetAckSuccess** / **fleetAckFailed** (and related templates) through ``MissionRunLoggingSubsystem`` with task/slot context from the assignment. | Run-local timeline — not yet rolled up into per-slot state machines. |
| ``FleetRecipeRunner`` | MC recipes use ``run(… source: issued.fleetDispatchSourceLabel …)``; ``FleetRecipeOutcome`` drives ``appendRecipeRunAckLogs`` (success vs failure reason on the run log). Escalations use ``MissionRunRecipeOperatorPromptBridge`` when ``MissionRunEnvironment`` has a run id. | Internal recipe run ids exist inside the runner; MRE does **not** mirror outcomes into ``FleetVehicleModel/Functions/recipeRunHistory`` (that capped ring is fed mainly by arm probes / Vehicle Inspector via ``FleetLinkService/recordRecipeRun`` — see **FVM `recipeRunHistory` v1** below in this README). |
| ``FleetCommandsCatalogue`` | Async ``invoke`` for catalogue atoms with the same **source** attribution; completion is log-only on the run today. | Same ``issued`` join as recipes. |
| **Hub telemetry** | ``FleetLinkService/hubTelemetryByVehicleID`` (``FleetHubVehicleTelemetry``): live pose, modes, mission items, etc., for maps and ``FleetVehicleOperationalModel``. | Resolve stream id from roster token → **pull-path** conformance (e.g. landed, mission cleared) alongside push evidence. |
| **FVM command queue / history** | ``FleetLinkService/executeVehicleCommand`` enqueues on ``FleetVehicleModel``; ``commandHistory`` retains recent rows with status updates from MAVSDK completions. | ``source`` distinguishes ``missionControl:…`` from other publishers — useful when a path issues raw RTL without a wrapping recipe. |

### Task-scoped abort / complete dispatch (entry + exit paths)

**Operator → scheduler → executor:** ``MissionRunEnvironment/abortMissionTask`` and ``completeMissionTask`` require ``running``/``paused``, ``sessionPhase == .executing``, and ``effectiveExecutionContextForDispatch()`` (otherwise warning ``abortMissionTaskNoDispatchContext`` / ``completeMissionTaskNoDispatchContext``). They capture context and call ``MissionRunSchedulingSubsystem/abortMissionTaskNow`` / ``completeMissionTaskNow``, which delegate to ``MissionRunExecutionSubsystem/performImmediateMissionTaskAbort`` / ``performImmediateMissionTaskComplete``. **Graceful** APIs (``abortMissionTaskGraceful`` / ``completeMissionTaskGraceful``) do **not** need dispatch context; they enqueue via ``abortMissionTaskAfterCycle`` / ``completeMissionTaskAfterCycle``. Delivery is ``MissionRunExecutionSubsystem/deliverPendingMissionTaskGracefulWindDownsIfNeeded`` when the mission cycle finishes for that task. Scheduling can refuse graceful with ``…SkippedWholeRunStopActive`` or ``…SkippedNoSlots``; on acceptance it logs ``…GracefulScheduled``.

**Per-assignment issuance:** ``assignmentsBoundToMissionTask`` defines bound slot ids; ``MissionRunPlannerSubsystem/buildAbortPlan`` and ``buildCompletePolicyWindDownCommands(limitedToAssignmentIDs:)`` supply ``MissionRunIssuedCommand`` rows (re-attributed ``issuer`` ``operator``). ``MissionRunCommandSubsystem/dispatchCommand`` routes raw vehicle vs catalogue vs recipe. When the batch needs ordering around mission clear, ``dispatchCommandsRespectingMissionClearBarrier`` walks commands **in order**: catalogue mission-clear atoms ``await`` ``awaitCatalogueMissionClearDispatchAndAckLogs``; **recipe** rows ``await`` ``awaitRecipeDispatchAppendingDispatchedThenAckLogs``; everything else dispatches synchronously via ``dispatchCommand`` — same helper used for whole-run immediate abort when the plan includes mission clear.

**Executor exit paths:**

| Outcome | Abort (now / graceful) | Complete (now / graceful) |
| --- | --- | --- |
| **No bound roster rows** | Warning ``missionTaskAbortNowSkippedNoSlots``; ``performImmediate…`` returns ``false``. Graceful: empty bound set after consuming pending → **silent** skip (no log). | Warning ``missionTaskCompleteNowSkippedNoSlots``; ``false``. Same silent skip pattern for graceful. |
| **Planner yields no commands for bound ids** | ``missionTaskAbortSkippedNoCommands`` / ``missionTaskAbortGracefulSkippedNoCommands``; no ``markMissionTaskAbortWindDownIssued``. | ``missionTaskCompleteSkippedNoCommands`` / ``missionTaskCompleteGracefulSkippedNoCommands``; no ``markMissionTaskCompleteWindDownIssued``. |
| **Dispatched** | ``markMissionTaskAbortWindDownIssued``, cancel deferred task mission starts, ``missionTaskAbortNowDispatched`` / ``missionTaskAbortGracefulDispatched``. If mission-clear barrier applies, ``true`` is returned **before** the async ``Task`` finishes; issued flag + log run **after** ``await``. | ``markMissionTaskCompleteWindDownIssued``, same cancel, ``missionTaskCompleteNowDispatched`` / ``missionTaskCompleteGracefulDispatched``. |

**Fleet follow-on:** ``dispatchCommand`` can still append error ``MissionRunEvent`` rows (invalid token, vehicle unresolved, command not queued). Catalogue async ack and fire-and-forget recipe ``Task`` paths remain **log/timeline** evidence (see table above). Recipe escalation UX uses ``MissionRunRecipeOperatorPromptBridge`` when the run has an id — there is no separate MRE-level dispatch **timeout** template on these paths today.

**Gap (explicit):** there is no ``assignmentID → lastRecipeRunID`` / ``policySucceeded`` aggregate on ``MissionRunEnvironment``; adding one is design work in ``TaskRosterAssignmentStatesToDo.md`` §2–§4.

**Not first-class in code today:** per-roster-**assignment** (slot) lifecycle enum — see ``TaskRosterAssignmentStatesToDo.md`` backlog.

### Operator actions → run state (summary)

| Operator / system action | Primary run transition | Notes |
| --- | --- | --- |
| **Begin run** (``beginRun``) | ``setup`` → ``running``, session → ``staging`` | Clears ``gracefulStopKind`` / ``completionKind`` / ``completedAt`` and ``clearMissionTaskScopedOrchestrationState()`` before ``setSessionPhase(.staging)``. |
| **Start execution** (MC start / ``startExecution``) | ``staging`` → ``executing``, ensures ``running`` | Clears cycle and queue state; dispatches staging pass. |
| **Pause / resume** | ``running`` ↔ ``paused`` | Session phase unchanged; task derivation re-runs. |
| **Whole-run abort now** | Stays ``running``/``paused``; ``executing`` → ``aborting`` | ``MissionRunSchedulingSubsystem/abortNow`` → ``performImmediateAbort`` → ``completeRun(… operatorWindDown: .abortProtocolPhase)``. |
| **Whole-run abort after cycle** | Same terminal as abort now | Queued at cycle boundary via ``gracefulStopKind`` + after-cycle batches in ``processMissionCycleFinished``. |
| **Whole-run complete now** | ``running``/``paused`` → ``recovery``, ``executing`` → ``recovery`` | ``completeNow`` → ``performImmediateComplete``. |
| **Whole-run complete after cycle** | → ``recovery`` | Same pattern as abort-after-cycle with ``operatorWindDown: .recoveryPhase``. |
| **Bounded mission autopilot finished** (all finite cycles done, no follow-on) | → ``recovery`` | ``completeRun(… oneOffAutopilotFinished)``. |
| **Mark run completed** (operator) | → ``completed`` / ``completed`` | ``markCompleted``; cancels scheduled tasks. |
| **Mark run failed** | ``completed`` / ``aborted`` | ``markFailed`` — task derivation uses the ``status.completed`` + ``sessionPhase.aborted`` branch. |
| **Reset run to setup** | → ``setup`` / ``draft`` | Clears triage ack sets, wind-down flags, cycles, events, plan, etc. |
| **Task abort now / after cycle / complete now / after cycle** | **No** session phase change by itself | Must be in ``executing``; updates pending map and/or wind-down issued sets per task; run stays ``executing`` until a whole-run path above fires. |

## Fleet Commands & Recipes architecture

Guardian commands vehicles through a strict three-layer stack. This is the
authoritative reference for the layout, conventions, and extension points; the
deferred **Stage E** (Vehicle Inspector recipe wizard) backlog lives in `NEXTVERSION.md`
(**Vehicle Inspector recipe wizard (Stage E) + deferred UX**). **MRE** preferential policy / between-cycles fleet rows route through
catalogue + ``FleetRecipeRunner`` (see **Mission** subsection under **Layer 1 subsystems** below).

### Layers (top → bottom)

| Layer | Owner | Responsibility |
| --- | --- | --- |
| **Layer 2 — Processes** | Operator wizard, MCR headless, plugin headless, LiveDrive | Run recipes; route escalations to the appropriate operator-prompt channel per a process-declared fallback policy. |
| **Layer 1 — Recipes (per Fleet subsystem)** | Calibration, Errors, Diagnose | JSON-authored declarative state machines: issue command → branch on response → retry / escalate / continue. Knows nothing about operators or UI. |
| **Layer 0 — Commands** | Core | Atomic, stack-translated commands in `command.do.* / command.get.* / command.cancel.*`. Each returns a normalised typed response. No knowledge of humans, recipes, missions, plugins. |

Direct `command.*` invocation is allowed but discouraged — every real flow goes through a recipe so response handling, branching, retries, escalation, and audit trail come for free.

### Source layout

- **Layer 0** — `Sources/GuardianHQ/Systems/Fleet/Subsystems/CommandsCatalogue/`
  - `FleetCommandName.swift` — `command.<addressing>.<verb>.<specifier>` validated identifier (verbs: `do | get | cancel`).
  - `FleetCommandResponse.swift` — closed outcome / error-kind / payload taxonomy.
  - `FleetCommandParameterSchema.swift` — parameter type kinds, declarations, validator.
  - `FleetCommandDescriptor.swift` — descriptor metadata: name, parameters, declared response kinds, retry hints, risk tier, composite `containsCommands`.
  - `FleetCommandsCatalogue.swift` — `@MainActor` registry singleton with `invoke(...)` pipeline.
  - `FleetCommandsCatalogueBootstrap.swift` — idempotent registration entry point.
  - `FleetCommandStackConverter.swift` + `Stacks/` — per-autopilot translation (`ArduPilot`, `PX4`, `Unknown`).
  - `Core/FleetVehicleCoreCommandRegistrations.swift` — core `command.fleet.vehicle.*` registrations.
- **Layer 1** — `Sources/GuardianHQ/Systems/Fleet/Subsystems/RecipesCatalogue/`
  - `FleetRecipeName.swift` — `recipe.<subsystem>.<specifier>` validated identifier (no verb segment).
  - `FleetRecipeParameterSchema.swift` — sibling of Layer 0's parameter validator.
  - `FleetRecipeRetryPolicy.swift` — policy struct + locked defaults + parser-time hard caps.
  - `FleetRecipeStepID.swift` — in-body author-chosen step identifier.
  - `FleetRecipePayloadPredicate.swift` — closed 8-kind predicate vocabulary (incl. `.stringMatches(regex:)`).
  - `FleetRecipeResponseMatcher.swift` — `success | error.<kind> | data(predicate) | timeout | cancelled | any`.
  - `FleetRecipeControlOutcome.swift` — `continue | branch | retry | succeed | fail | escalate`.
  - `FleetRecipeEscalation.swift` — escalation reason kinds (string-backed, extensible) + closed resumption-verb set.
  - `FleetRecipeStep.swift` + `FleetRecipeBody.swift` — `invokeCommand` / `invokeRecipe` steps, ordered body, entry step, overall budget.
  - `FleetRecipeBodyParser.swift` — JSON decode + structural validation (all errors surfaced in one pass).
  - `FleetRecipeDescriptor.swift` — descriptor with optional `body: FleetRecipeBody?` and optional `cancelRecipe: FleetRecipeName?` cleanup hook.
  - `FleetRecipesCatalogue.swift` + `FleetRecipesCatalogueBootstrap.swift` — singleton registry + idempotent entry point.
  - `FleetRecipeRunID.swift` + `FleetRecipeAuditTrace.swift` + `FleetRecipeOutcome.swift` + `FleetRecipeEscalationEvent.swift` — runner support types.
  - `Fleet/Utilities/FleetRecipeOutcomeOperatorToast.swift` — short completion toast copy for any surface that finishes a catalogue recipe (`ToastCenter` + ``GuardianFeedbackSeverity``).
  - `FleetRecipeRunner.swift` — `@MainActor` singleton runner: per-vehicle execution, step machine, retry / branch / escalate / cancel, audit trace; publishes ``FleetRecipeRunner/wizardProgressByVehicleID`` during ``run`` for Vehicle Inspector progress UI, and ``FleetRecipeRunner/wizardEscalationByVehicleID`` when a run uses ``FleetRecipeRunner/vehicleInspectorWizardEscalationHandler(for:)`` and hits Layer 2 escalation. Wizard snapshots and escalation events carry the **top-level** run id even during nested ``invokeRecipe`` so ``FleetRecipeRunner/cancel(runID:)`` matches the procedure banner.
  - `FleetRecipeWizardProgressSnapshot.swift` — live per-vehicle procedure progress (step ordinal, activity line) for Stage E wizard chrome.
  - `FleetRecipeWizardEscalationSnapshot.swift` — human headline/detail + allowed resumption verbs for inline wizard escalation (Vehicle Inspector); resolved via ``FleetRecipeRunner/submitWizardEscalationVerb(vehicleID:verb:)`` or ``FleetRecipeRunner/cancel(vehicleID:)`` / ``FleetRecipeRunner/cancel(runID:)`` (auto-unblocks the escalation await).
  - `FleetRecipeBodyLoader.swift` — hybrid loader: resolves a per-recipe JSON body from a bundle resource and decodes via `FleetRecipeBodyParser`; subsystem `registerAll()` calls it with its own `bodiesSubdirectoryName` and `Bundle.module`.
- **Layer 1 subsystems** — each subsystem owns a sibling directory under `Sources/GuardianHQ/Systems/Fleet/Subsystems/` containing a `@MainActor` registrations entry point invoked once by `FleetRecipesCatalogueBootstrap` plus a uniquely-named per-recipe-bodies directory. Directory names must be unique inside the bundle because SPM flattens `.copy(<dir>)` resources to the bundle root. Today's subsystems:
  - `Calibration/FleetCalibrationRecipeRegistrations.swift` + `Calibration/CalibrationBodies/*.json` — calibration recipes (`recipe.fleet.calibrate.*`) plus the diagnose recipes (`recipe.fleet.diagnose.*`) that share the same bodies directory. The entry-point enum exposes `bodiesSubdirectoryName` (`"CalibrationBodies"`). Stage C ships **25 recipes** total — 22 calibrations spanning every `do.calibrate.*` command surface (see `Sources/GuardianHQ/Systems/Fleet/Subsystems/Calibration/CalibrationBodies/_AUTHORING.md` for the per-recipe breakdown): one atomic cleanup recipe (`recipe.fleet.calibrate.cancel`), eight Pattern-A interactive cals (`compass`, `accelerometer`, `gyro`, `baro`, `level`, `airspeed`, `esc`, `rc`/`rc.trim`), three Pattern-B stack-asymmetric cals with explicit `notImplemented → fail` matchers (`compass.motor`, `baro.temperature`, `gimbal`), three Pattern-C discoverability shells whose underlying converter paths are `notImplemented` in v1 (`rangefinder`, `flow`, `vision`), and six Pattern-D param-driven writes using caller→step parameter references (`compass.declination`, `battery.{voltage,current,capacity}`, `servo`, `gimbal.neutral`); plus 3 Pattern-E diagnose recipes — `recipe.fleet.diagnose.cancel` (best-effort disarm cleanup), `recipe.fleet.diagnose.armprobe` (arm → disarm probe that classifies the common autopilot refusal kinds), and `recipe.fleet.diagnose.armprobe.hold` (arm-only terminal success for callers that must leave the vehicle armed). Vehicles / Mission Control preflight probes run these through ``FleetRecipeRunner`` from ``MissionControlStore`` (`armprobe` by default; `armprobe.hold` when `leaveArmed` is true or for Mission Control start-run roster checks).
  - `Errors/FleetErrorRecipeRegistrations.swift` + `Errors/ErrorBodies/*.json` — error-fix recipes (`recipe.fleet.errors.fix.*`). Ships **1 recipe** in Stage C: `recipe.fleet.errors.fix.calibrationrequired` — the first composite (invokeRecipe-bearing) recipe in the catalogue. Four sequential `invokeRecipe` steps drive a compass → accelerometer → gyro calibration sweep then verify with `recipe.fleet.diagnose.armprobe`; the recipe declares the four children in `containsRecipes` so the catalogue's depth check enforces 1-level composition in both directions. The entry-point enum exposes `bodiesSubdirectoryName` (`"ErrorBodies"`).
  - `Mission/FleetMissionRecipeRegistrations.swift` + `Mission/MissionBodies/*.json` — MRE / Mission Control recipes: `recipe.fleet.do.mission.upload.start` (upload → arm → start), `recipe.fleet.do.mission.upload.start.item` (upload → jump to `missionStartItemIndex` → arm → start, for post-commit reserve handoff when displaced hub progress is above zero), `recipe.fleet.do.return.home` (single-step `command.fleet.vehicle.do.return.home` / RTL), and `recipe.fleet.vehicle.do.park` (single-step `command.fleet.vehicle.do.park` for cleanup / park shells). Descriptor metadata is Swift-authored; bodies load from JSON like other subsystems, but **if bundle JSON is missing** (some Xcode / host layouts), registration uses a **compiled-in `FleetRecipeBody`** for each of those names so the catalogue never stays empty. The entry-point enum exposes `bodiesSubdirectoryName` (`"MissionBodies"`).
  - **MRE policy & between-cycles dispatch** — Preferential **abort** / **complete** tactics (excluding nearest-open-map-point, which still uses the planner-built move-point-park recipe) and **continuous-task between-cycles** actions emit ``MissionRunFleetDispatch`` **recipe** (RTL → `recipe.fleet.do.return.home`) or **catalogue** atoms (`command.fleet.vehicle.do.loiter`, `.do.park`, `.do.land` for between-cycle land). ``MissionRunCommandSubsystem`` resolves them through ``FleetRecipeRunner`` / ``FleetCommandsCatalogue``; recipe escalations during a run use ``MissionRunRecipeOperatorPromptBridge`` when ``MissionRunEnvironment`` has a run id. The legacy ``MissionRunIssuedCommand`` convenience that wraps a raw ``FleetVehicleCommand`` remains for other call sites.
  - These subsystem registration entry points are idempotent — calling `registerAll()` twice is a no-op by the catalogue's per-name overwrite rule, and the bootstrap's one-shot latch short-circuits subsequent `ensureRegistered()` calls.
- **Telemetry recipe directory** — `FleetTelemetryFieldCatalog.systemRecipes` is a per-`FleetCalibrationSystemID` map of the calibration + error-fix recipe names that act on each system. The Vehicle Inspector (Calibration tab) resolves each citation through `FleetRecipesCatalogue` and shows **one Run launcher per recipe** in directory order (`Calibrate` then `Fix` sections), wired to `FleetRecipeRunner.shared.run(...)` with ``FleetRecipeRunner/vehicleInspectorWizardEscalationHandler(for:)`` and source `vehicleInspector.recipe.<system>`. While a run is active, a pinned strip shows **step progress** (dispatch index + authored step count), the **current activity line** (command or nested procedure label), and **Cancel** (which also unblocks any pending Layer 2 escalation await). When a step escalates, a second panel shows **human headline/detail** (e.g. rotate-the-vehicle copy for `rotateDrone`) plus **Acknowledge / Retry / Skip / Abort** buttons for the matcher’s `allowedVerbs`. Stage D’s router and other surfaces can still subscribe to the same escalation events separately; this path is the inline wizard contract. Plugin-only **extension-registry** controls remain a fallback only when a system has **no** directory recipes. The directory references recipes by **name only**, so recipes themselves stay owned by their subsystem registrations. `FleetRecipesCatalogueBootstrap.ensureRegistered()` calls `FleetTelemetryFieldCatalog.validateRecipeReferences(against:)` after every subsystem has registered and logs a fault for any citation that didn't resolve — a typo in a directory entry fails at app start rather than at menu-render time. A miss is a soft failure (the inspector just skips the menu entry); the validator returns the misses sorted `(system, recipe)` so log output stays stable. Live-mission gating for catalogue **Run** rows uses the same stream-bound signal as the Vehicle Inspector **Recipe locked** header for non-`safeInLiveMission` tiers; see ``FleetRecipeDescriptor/vehicleInspectorLaunchBlockedDuringLiveMission(isVehicleInLiveMission:)``. While a run is active, ``VehicleCalibrationView`` repaints the matching calibration marker from live ``FleetRecipeRunner`` progress when ``FleetTelemetryFieldCatalog/calibrationSystemID(forTelemetryDirectoryRecipe:)`` resolves the top-level recipe to exactly one directory system (shared `errorFix` citations skip canvas emphasis).
- **Vehicle Inspector → FVM recipe-run history** — When a **Run** completes, ``VehicleInspectorRecipeRunHistoryMapper`` maps ``FleetRecipeOutcome`` into the v1 outcome envelope and ``FleetLinkService/recordRecipeRun`` appends with ``RecipeRunHistoryKind/vehicleInspectorCatalogueRecipe``. Arm probes from the calibration modal use the same API with ``RecipeRunHistoryKind/preflightArmProbe``. Failed catalogue runs stamp remediation `patternId` values `wizard.cal.<FleetCalibrationSystemID>` so ``FleetCalibrationCollection`` can overlay the matching canvas marker (same mechanism as arm-probe patterns).
- **Recipe source-of-truth: hybrid.** Descriptor metadata (`FleetRecipeDescriptor` — name, human label, risk tier, retry policy, parameters, prerequisites, escalation expectations, `cancelRecipe`) is authored as a **Swift literal** inside the subsystem's `registerAll()` so the compiler enforces every field rename, type change, and namespace claim. Recipe **bodies** (steps, matchers, branches, escalation outcomes) live as **per-recipe JSON files** under that subsystem's bodies directory (e.g. `CalibrationBodies/<recipe.name>.json`), loaded by `FleetRecipeBodyLoader` (`Bundle.module` resource → `FleetRecipeBodyParser.decode(jsonData:)` → `Result<FleetRecipeBody, FleetRecipeBodyLoadError>`). Catalogue-level structural validation (matchers, branch targets, registered references, regex compile, budget caps, 1-level composition depth) still happens inside `FleetRecipesCatalogue.register(...)`. Subsystem `registerAll()` calls `FleetRecipeBodyLoader.load(...)` with its own `bodiesSubdirectoryName` and attaches the loaded body to the Swift descriptor literal before `FleetRecipesCatalogue.shared.register(...)`; **if JSON load fails, most subsystems skip registration** (logging the diagnostic). **Mission** recipes are the exception: `FleetMissionRecipeRegistrations` supplies a **Swift fallback body** identical to the JSON so `recipe.fleet.do.mission.upload.start`, `recipe.fleet.do.mission.upload.start.item`, and `recipe.fleet.do.return.home` always register when commands and catalogue bootstrap are ready. This keeps the iteration-heavy step graph data-only where the resource is present while compile-time safety still guards the metadata layer. Plugin contributions follow the same shape — Swift descriptor + uniquely-named JSON bodies directory (see ``Sources/GuardianHQ/Plugins/PLUGIN_FLEET_CONTRIBUTIONS.md`` for manifest claims and layout).

### FVM `recipeRunHistory` v1 (per-vehicle ring)

- **Cap:** newest-first **3** rows on ``FleetVehicleModel/Functions`` (``recipeRunHistoryCap``).
- **Row model:** ``RecipeRunHistoryEntry`` — ``RecipeRunHistoryKind`` (`preflightArmProbe`, `vehicleInspectorCatalogueRecipe`, `pluginOther`), free-form `source` (e.g. `calibrationModal.manual`, `vehicleInspector.recipe.core.barometer`), and v1 **outcome** fields in ``SingleVehiclePreflightProbeResult`` (`passed`, `armedDuringProbe`, `detail`, optional `remediationAdvice`). UI and new writers should use **kind**, not string-matching on `source`, for semantics.
- **Write API:** ``FleetLinkService/recordRecipeRun(vehicleID:source:kind:outcome:)`` and ``FleetLinkService/clearRecipeRuns(vehicleID:)`` (symmetric record / clear). When ``FleetCalibrationCollection.make`` rebuilds, a private overlay pass merges the newest **failed** row’s remediation into the matching calibration marker.
- **Storage:** in-memory on ``FleetLinkService`` vehicle models (not persisted across relaunch in v1).
- **Canvas overlay:** ``FleetCalibrationCollection`` inspects only the **newest** entry; on failure with remediation, `patternId` maps to a calibration system (existing arm-probe patterns plus `wizard.cal.<FleetCalibrationSystemID>` for inspector catalogue failures).

### Layer 1 subsystem authoring checklist

Use this checklist when adding a new Fleet recipe subsystem or extending an existing one:

1. **Create the subsystem shell:** add `Sources/GuardianHQ/Systems/Fleet/Subsystems/<Subsystem>/`, a `@MainActor` `<Subsystem>RecipeRegistrations` entry-point enum, and a uniquely named bodies directory such as `<Subsystem>Bodies`. Add that bodies directory to `Package.swift` with `.copy(...)`; do not name it only `Bodies` because SwiftPM flattens copied resources to the bundle root.
2. **Register through bootstrap:** expose `registerAll()` and call it once from `FleetRecipesCatalogueBootstrap.ensureRegistered()`. Registration must be idempotent; repeated app startup hooks or tests should leave the same descriptor set.
3. **Author descriptor metadata in Swift:** every `FleetRecipeDescriptor` must declare `name`, `humanLabel`, `humanDescription`, `parameters`, `riskTier`, `expectedDuration` when useful, `prerequisites`, `appliesToSystems`, retry policy, optional `pluginID`, `containsRecipes`, loaded `body`, and optional `cancelRecipe`. Keep labels operator-readable and descriptions specific enough for the inspector / wizard.
4. **Author bodies in JSON:** one file per recipe named `<recipe.name>.json`, loaded by `FleetRecipeBodyLoader` with `recipeName.rawValue`. Bodies declare one `entryStepID`, a bounded `overallBudgetSeconds`, and ordered `invokeCommand` or `invokeRecipe` steps. Prefer data-only bodies; use Swift only when the DSL cannot express the flow.
5. **Match per stack deliberately:** when a command is supported on one stack and unavailable on another, add an explicit `error(notImplemented) -> fail` matcher with a clear detail before the final `any` matcher. Unsupported-stack behavior should never be an accidental fall-through.
6. **Choose escalation vs fail:** escalate only when a process or operator could reasonably resume (`rotateDrone`, `holdStill`, `confirmInLiveMission`, etc.). Fail for structural limits, unavailable transports, invalid parameters, read-back mismatch, authority gates, or states where retrying needs a different process decision.
7. **Wire telemetry directory entries:** recipes are surfaced by `FleetTelemetryFieldCatalog.systemRecipes`, keyed by `FleetCalibrationSystemID`, with separate `calibrate` and `errorFix` lists. The directory references recipe names only; ownership stays with the subsystem registration. Bootstrap validation logs any missing citations.
8. **Add tests with the feature:** cover registration idempotency, descriptor metadata, body structure, matcher ordering, composition depth, parameter references, telemetry-directory references, and runner behavior when the recipe changes execution semantics. Add SITL smoke coverage for stack-facing happy paths when practical.

### Bootstrap order

Both catalogues self-register at app start, in this order, from `GuardianHQApp.swift`:

```swift
GuardianPluginBootstrap.ensureRegistered()
FleetCommandsCatalogueBootstrap.ensureRegistered()
FleetRecipesCatalogueBootstrap.ensureRegistered()
```

All three are idempotent; adding new core / subsystem registrations is a matter of plugging into the appropriate bootstrap, not reordering app startup. ``GuardianPluginBootstrap`` validates each ``GuardianPluginManifest`` before registration: ``publishedCommandNamespaces`` / ``publishedRecipeNamespaces`` must sit under the plugin’s ``GuardianPluginID/fleetNamespaceTail``; ``invokedCommandNamespaces`` / ``invokedRecipeNamespaces`` are shape-checked dotted `command.*` / `recipe.*` prefixes (not tied to the plugin’s publish tree). **Paladin** keeps those four arrays **empty** — it uses core fleet commands/recipes via Vehicle Inspector, Mission Control, LiveDrive, and MRE-style surfaces, not parallel Paladin-owned catalogue entries; see ``Sources/GuardianHQ/Plugins/PLUGIN_FLEET_CONTRIBUTIONS.md`` for plugins that *do* publish or invoke fleet names. After bootstrap, ``FleetCommandsCatalogue`` / ``FleetRecipesCatalogue`` refuse descriptors whose ``pluginID`` is set unless the name is covered by that plugin’s publish-namespace list in the registry (core entries keep ``pluginID == nil``). Plugin-owned recipe bodies are validated (and the runner + ``FleetCommandsCatalogue/invoke(invokingPluginID:)`` enforce) that every ``invokeCommand`` / ``invokeRecipe`` step sits under the plugin manifest’s ``invokedCommandNamespaces`` / ``invokedRecipeNamespaces`` claims.

### Stage G — automated test coverage

Deterministic unit tests live under `Tests/GuardianHQTests/` (SwiftPM test target path). This is the canonical map for **Commands / Recipes / stack taxonomy** regression coverage (expand here when you add a new suite):

| Area | Primary test types |
| --- | --- |
| Layer 0 name + registration | ``FleetCommandNameTests``, ``FleetCommandsCatalogueBootstrapTests`` |
| Stack converters + **response taxonomy** (normalise heuristics) | ``FleetCommandStackConverterNormaliseTests``, ``FleetCommandStackConverterNotImplementedTests``, ``FleetCommandStackConverterCoverageTests``, ``FleetCommandStackTaxonomyParityTests`` |
| Layer 0 parameters | ``FleetCommandParameterValidatorTests`` |
| Layer 1 names + registration + composition caps | ``FleetRecipeNameTests``, ``FleetRecipesCatalogueRegistrationTests``, ``FleetRecipesCatalogueBootstrapTests``, ``FleetCalibrationRecipeRegistrationsTests``, ``FleetErrorRecipeRegistrationsTests`` |
| JSON body decode + validation | ``FleetRecipeBodyParserTests``, ``FleetRecipeBodyLoaderTests``, ``FleetRecipeStepAndBodyTests`` |
| Matchers / predicates / retry policy | ``FleetRecipeResponseMatcherTests``, ``FleetRecipePayloadPredicateTests``, ``FleetRecipeRetryPolicyTests``, ``FleetRecipeParameterValidatorTests`` |
| **Runner** (branch, retry, escalate, cancel, budget, conflict, **live-mission gate**, wizard chrome) | ``FleetRecipeRunnerTests``, ``FleetRecipeEscalationTests`` |
| Live-mission **descriptor** policy helper | ``FleetRecipeDescriptorLiveMissionPolicyTests`` |
| Outcome → operator toast | ``FleetRecipeOutcomeOperatorToastTests`` |
| Telemetry directory ↔ recipe citations | ``FleetTelemetryFieldCatalogRecipeDirectoryTests``, ``FleetTelemetrySystemRecipeDirectoryTests`` |
| Plugin manifest + catalogue publish/invoke claims | ``GuardianPluginManifestNamespaceTests``, ``PluginNamespaceCatalogueRegistrationTests`` |
| Preflight / MC mapping helpers | ``MissionControlPreflightRecipeOutcomeMapperTests`` |
| MRE policy dispatch shape (abort / complete / between-cycles) | ``MissionRunFleetDispatchPolicyMappingTests`` |
| Recipe escalation → operator prompt channel | ``RecipeEscalationOperatorPromptIntegrationTests`` |
| Opt-in SITL (Layer 1 recipes + composite + cancel) | ``GuardianHQSitlSmokeTests`` — Pattern-A `test_recipeFleetCalibrate{Compass,Accelerometer,Gyro,Baro,Level}_endToEnd_*`, `test_recipeFleetDiagnoseArmprobe_endToEnd_*`, `test_recipeFleetErrorsFixCalibrationRequired_endToEnd_*`, `test_cancelMidRecipe_runsDeclaredCancelRecipe_cancelCalibration_sitlBothStacks` |

**Opt-in SITL:** ``GuardianHQSitlSmokeTests`` exercises Layer 0 against real ArduPilot + PX4 when `GUARDIAN_RUN_SITL_SMOKE=1` (see **SITL command-catalogue smoke tests** above and `scripts/run_sitl_smoke_tests.sh`). Layer 1 coverage listed in the table above ships in the same suite. Stage G **deterministic** integration for recipe escalation + prompts is in ``RecipeEscalationOperatorPromptIntegrationTests`` (table row above).

### Locked architectural decisions

These are baked into the validators / registries. Changing one is an explicit, reviewed decision.

| Decision | Value |
| --- | --- |
| Reserved Layer 0 verbs | `do`, `get`, `cancel` only; `subscribe` is deferred. |
| Composition depth | `recipe → recipe` at exactly **1 level**; `command → command` at exactly **1 level**. Max shape `recipe → recipe → command → command`. No cycle detection because depth is bounded. |
| Recipe outcome | Binary — `succeeded` **or** `failed(failingCommandPath, lastResponse)`. No partial-success. |
| Authoring format | JSON-first. Swift escape hatch only when the DSL genuinely cannot express a flow. |
| Telemetry catalogue role | **Directory only**, never an owner. System entries reference recipe paths; recipes live in their Calibration / Errors subsystem catalogues. |
| Plugin contribution path | Plugins extend Layer 1 and Layer 2 only. They do **not** register `command.*` entries except in the rare custom-comms exception (`command.do.<plugin>.*`). |
| Universal bus scope (v1) | `command.fleet.*` only. `command.mc.*`, `command.plugin.*` are deferred. |
| Live-mission gate | Recipes declare a risk tier; the runner enforces it via the existing live-mission gate primitive — no new gate machinery. |

### Layer 0 conventions

- **Name shape:** `command.<addressing-path>.<verb>.<specifier-path>` (e.g. `command.fleet.vehicle.do.calibrate.compass`).
- **Lexical rules:** lowercase ASCII letters, digits, dots; bounded length 128.
- **Response taxonomy:** every stack converter must classify every raw outcome into the closed `FleetCommandErrorKind` set — recipes branch reliably on `error.<kind>` without parsing free-form strings.
- **Descriptor metadata:** declared response kinds, retry hints (read by Layer 1 only — Layer 0 itself is single-shot), risk tier, optional `containsCommands` for composites.

### Layer 1 conventions

- **Name shape:** `recipe.<subsystem>.<specifier-path>` (e.g. `recipe.fleet.calibrate.compass`); same lexical rules as commands; no verb segment.
- **Catalogue retry default** (used when neither a step nor a recipe declares its own): `1 retry × 250 ms fixed delay × { timeout, .noSession, .autopilotBusy }`. Authority and validation failures **never** retry by default.
- **Retry caps** (parser hard-fails on violations): `maxAttempts ≤ 5`, `delaySeconds ≤ 5s`, worst-case additional time ≤ 15s. A descriptor may opt out with `relaxRetryCaps: true` — the registry then logs-and-warns instead of rejecting.
- **Payload predicate vocabulary** (closed 8-kind set): `keyValueEquals`, `keyValuePresent`, `boolEquals`, `stringEquals`, `stringMatches(regex)`, `integerCompare(op, value)`, `doubleCompare(op, value)`, `stringListContains`. Regex patterns are compiled at parse time.
- **Escalation reason** (closed top-level): `operatorActionRequired(kind) | unrecoverableFailure(kind) | confirmation(kind)`. The kinds themselves are string-backed extensible namespaces (`Notification.Name`-style) — plugins can declare new kinds without core changes.
- **Resumption verbs** (closed set): `acknowledge | retry | skip | abort`. A matcher's `allowedVerbs` filters which verbs the runner accepts when resuming.
- **Recipe body budget:** `60s` default, hard cap `600s`.
- **Parser-time validation** (single pass, every error surfaced): step-ID uniqueness; entry-step exists; matcher lists non-empty; `.any` matcher only in final position and never duplicated; `.branch(stepID:)` targets exist in the same body; invoked commands and recipes are registered; **1-level recipe-composition depth**; regex predicates compile; overall budget positive and within cap; per-step retry policy within caps unless the descriptor opts out.

### Layer 1 runner

`FleetRecipeRunner.shared` is the `@MainActor` singleton that drives a registered recipe body end-to-end. It is the single execution surface for Stage E's wizard, future Stage D operator-prompt routing, and any subsystem-level autonomous flow that runs **catalogue** recipes (including flows **started headlessly** by assistants such as Paladin, which still execute **core** `recipe.fleet.*` bodies rather than Paladin-owned fleet descriptors — operators interact through existing Mission Control / inspector surfaces, not Paladin UI). Source: `Sources/GuardianHQ/Systems/Fleet/Subsystems/RecipesCatalogue/FleetRecipeRunner.swift`.

- **Public surface:** `run(recipe:parameters:vehicleID:source:fleetLink:escalationHandler:) async -> FleetRecipeOutcome`, `cancel(vehicleID:) -> Bool`, and `cancel(runID:) -> Bool` (targets the top-level slot when the id matches wizard chrome). The runner does not own a `FleetLinkService` — callers pass it through `run(...)` and the runner forwards it to `FleetCommandsCatalogue.invoke(...)` for dispatch.
- **Concurrency model:** **one active run per vehicle, refuse-on-conflict.** A second `run(...)` targeting a vehicle that already has an active run returns `.failed(detail: "… already executing recipe …")` immediately. Callers handle retry/queueing. `invokeRecipe` step expansions bypass this gate because they're part of the parent run's accounting.
- **Outcome shape:** binary `FleetRecipeOutcome.succeeded(detail, payload, trace)` / `.failed(failingCommandPath, lastResponse, detail, trace)`. Cancelled and budget-breach scenarios are reported as `.failed` variants with attribution in `detail` (`"cancelled"`, `"recipe budget exceeded (60s)"`, etc.).
- **Step machine:** matchers are evaluated top-to-bottom against the dispatched step's response; the first match wins and produces a `FleetRecipeControlOutcome`. `.continueToNextStep` advances by index; `.branch(stepID:)` jumps to a sibling step; `.retry` re-invokes the same step (bounded only by the recipe's overall budget); `.succeed` / `.fail(detail)` terminate the run; `.escalate(reason, allowedVerbs)` suspends until the escalation handler returns a resumption verb.
- **Retry policy:** auto-retries inside a single dispatch consume the step-level (or descriptor-level) `FleetRecipeRetryPolicy`. The catalogue's locked default (`1 retry × 250 ms × {timeout, .noSession, .autopilotBusy}`) applies when neither is specified. Explicit `.retry` control outcomes are authoring-driven and intentionally **not** counted against `maxAttempts` — they're bounded by `FleetRecipeBody.overallBudgetSeconds`.
- **Cancellation:** `cancel(vehicleID:)` or `cancel(runID:)` (same top-level slot when the run id matches) sets a flag on the active run. The in-flight Layer 0 command runs to completion (Layer 0 does not expose mid-flight cancel in v1), then the runner returns `.failed(detail: "cancelled")`. If the descriptor declares `FleetRecipeDescriptor.cancelRecipe`, that recipe is dispatched with empty parameters as cleanup **before** the parent outcome is returned — vehicle-side calibration cancel / disarm cleanup for interactive cals and diagnose flows. Cancel during cleanup is a no-op.
- **Live-mission gate:** consulted **only at top-level `run(...)` entry** (not at `invokeRecipe` child boundaries — the parent recipe's tier is authoritative). Wiring: `FleetRecipeRunner.shared.liveMissionGate` is a `(vehicleID) -> Bool` closure installed once at app start by `RootView.onAppear` against `MissionControlStore.isVehicleStreamUsedInLiveMission(...)`. When the gate fires and `allowDuringLiveMission == false`: `.groundOnly` recipes refuse with `"… is groundOnly; vehicle … is in a live mission. Pass allowDuringLiveMission=true to override."`; `.confirmInLiveMission` recipes refuse with `"… requires operator confirmation …"`; `.safeInLiveMission` recipes ignore the gate. Callers (Stage E wizard, MCR reserve-deploy / get-back-online flows) collect operator confirmation themselves and re-invoke with `allowDuringLiveMission: true`. A `nil` gate disables enforcement (tests, surfaces without a Mission Control store).
- **`cancelRecipe` field on the descriptor:** optional `FleetRecipeName?`. The catalogue rejects the parent registration if the cleanup recipe is unregistered, has its own `cancelRecipe`, or is composite (`containsRecipes` non-empty). v1 supports **one cleanup recipe per parent**; per-step cleanup hooks are a Stage B2 follow-up.
- **Escalation handler:** `FleetRecipeEscalationHandler` is `@MainActor (FleetRecipeEscalationEvent) async -> FleetRecipeResumptionVerb`. Callers pass a handler to `run(...)`; absent that, the runner uses `FleetRecipeRunner.shared.defaultEscalationHandler` (defaults to `.abort`, replaced by Stage D's router at app start). The runner **rejects** any verb that is not a member of the escalating matcher's `allowedVerbs` list (the run fails with attribution).
- **Audit trace:** `FleetRecipeAuditTrace` attaches to every outcome. One entry per dispatched step (retries within a single dispatch collapse into the entry's `attempt` count; explicit `.retry` outcomes produce additional entries because they re-enter the step). Each entry carries the step ID, the kind (`command` / `recipe`), attempt count, response, control outcome, and timestamp. The trace is **flat** — child-recipe expansions produce a single parent entry rather than a nested trace.
- **Body-less descriptors are refused at entry.** Stage C registers descriptors and bodies in independent passes; the runner short-circuits a body-less recipe with `.failed(detail: "… has no body …")` rather than silently succeeding on an empty step list.

### Stage E — Vehicle Inspector wizard contract (operators + other surfaces)

This is the **locked behaviour** for catalogue-driven calibration recipes in the Vehicle Inspector, and the **integration seams** Mission Control Run (MCR) and LiveDrive reuse when they drive the same runner.

#### Operator contract (Vehicle Inspector)

- **Entry:** Calibration tab → per-system **Calibrate** / **Fix** lists from `FleetTelemetryFieldCatalog.systemRecipes` → **Run** on a resolved `FleetRecipeDescriptor` (parameterised recipes stay disabled until collection UI exists).
- **Live mission:** Non-`safeInLiveMission` launches are blocked while the vehicle’s stream is bound to an active Mission Control run — same signal as the modal **Recipe locked** header and disabled Run rows (`FleetRecipeDescriptor/vehicleInspectorLaunchBlockedDuringLiveMission(isVehicleInLiveMission:)`).
- **During a run:** `FleetRecipeRunner` publishes ``FleetRecipeWizardProgressSnapshot`` on ``wizardProgressByVehicleID`` — step ordinal / total, current step id, activity line (command or nested procedure label), and a **top-level** ``FleetRecipeRunID`` even inside `invokeRecipe` so **Cancel** calls ``cancel(runID:)`` against the procedure the operator started. A pinned **procedure** strip shows progress; **Cancel** stops at the next step boundary (in-flight Layer 0 command still completes), unblocks any pending escalation await, and runs the descriptor’s **`cancelRecipe`** cleanup when declared (e.g. `recipe.fleet.calibrate.cancel` on interactive cals).
- **Layer 2 escalation:** Matchers that `.escalate` use ``FleetRecipeRunner/vehicleInspectorWizardEscalationHandler(for:)``, which mirrors ``FleetRecipeEscalationEvent`` into ``wizardEscalationByVehicleID`` until the operator picks a resumption verb via ``submitWizardEscalationVerb(vehicleID:verb:)`` or cancels. Copy comes from ``FleetRecipeEscalationReason/wizardInlineCopy()`` on the reason kind.
- **After a run:** Outcomes are mapped with ``VehicleInspectorRecipeRunHistoryMapper`` and stored via ``FleetLinkService/recordRecipeRun`` (``RecipeRunHistoryKind/vehicleInspectorCatalogueRecipe``); the newest failed row can overlay the calibration canvas when remediation maps to a system. Arm-probe flows use the same ring with ``RecipeRunHistoryKind/preflightArmProbe``.
- **Completion toast:** One short line from ``FleetRecipeOutcomeOperatorToast`` (recipe label + done, stopped, or couldn't complete). Raw runner ``detail`` belongs in logs / trace / FVM history — not in the toast.

#### Integration points — MCR and LiveDrive

- **Shared executor:** Both surfaces call the same ``FleetRecipeRunner/shared.run(recipe:parameters:vehicleID:source:fleetLink:allowDuringLiveMission:escalationHandler:)``. Pass the live ``FleetLinkService`` from the session; pick a **distinct `source` string** per surface (e.g. `missionControl.recipe.<context>`, `liveDrive.recipe.<context>`) so logs and FVM history stay attributable.
- **Escalation:** Inline wizard chrome is optional. For headless or panel-first flows, supply ``defaultEscalationHandler`` (or the Stage D router when installed) instead of ``vehicleInspectorWizardEscalationHandler(for:)``. Routed prompts use ``OperatorPromptDeliveryTarget`` addressing — ``mcrPromptPanel(missionRunID)``, ``liveDrivePromptPanel(missionRunID:vehicleID:)``, and optionally ``vehicleInspectorWizardPanel(vehicleID:recipeRunID:)`` when the operator may be on the inspector — see **Operator-prompt delivery targets** below. Fill ``OperatorPromptTarget`` as fully as the publisher can (vehicle, run, task) so filters match reliably.
- **Live mission override:** After operator confirmation in-product, re-invoke the same recipe with `allowDuringLiveMission: true` using the same gate strings the runner returns on refusal.
- **Authority (LiveDrive):** Control commands and manual takeover remain gated by ``FleetLinkService`` live-drive session rules; read-only / calibration-style recipes still need an explicit product decision if they should run while another surface holds control.
- **Cancellation:** Prefer ``cancel(runID:)`` when the UI holds the active snapshot id; ``cancel(vehicleID:)`` remains the slot-level equivalent.
- **Completion toasts:** When MCR or LiveDrive surfaces show recipe completion, reuse ``FleetRecipeOutcomeOperatorToast`` so copy stays consistent with the inspector.

### Operator-prompt channel — pre-existing hooks

Stage D will wire a single app-layer `OperatorPromptCenter` + `OperatorPromptRouter`. These are the **seeds already in code** that Stage D plugs into rather than reinventing; new prompt origins should reuse these surfaces rather than grow parallel ones.

- **Recipe-side escalation contract.** `FleetRecipeEscalationEvent` carries `runID`, `recipe`, `vehicleID`, `stepID`, `reason` (closed top-level `operatorActionRequired | unrecoverableFailure | confirmation`, each with string-backed extensible kinds), `allowedVerbs`, and `lastResponse`. `FleetRecipeEscalationHandler` is `@MainActor (FleetRecipeEscalationEvent) async -> FleetRecipeResumptionVerb` and is the install point for Stage D's router. `FleetRecipeResumptionVerb` is the closed transport (`acknowledge | retry | skip | abort`) every prompt resolves to — custom options layer on top of it.
- **MRE Rules-of-Engagement seeds.** `MissionRunEngagementAction` (`rtl | land | forceDisarm | swapInReserve`) × `MissionRunEngagementDisposition` (`autonomous | ask | defer | forbidden | handoff`) are stored on `MissionRunPolicies.engagement`, editable through `updateMissionEngagementRules` / `updateMissionEngagementDisposition` in `MissionRunEnvironment+PolicyAPI.swift`, and audit-trace logged via `policyAuthorityEditApplied` / `policyAuthorityEditDenied`. The dispositions `.ask`, `.defer`, `.handoff`, `.forbidden` have **no consumer today** — they are declarative input to Stage D's router. `resolvedEngagementDisposition(for:)` is the lookup used by future planner gating; `.ask` and `.defer` publish through `OperatorPromptCenter`, `.handoff` invokes a LiveDrive takeover, `.forbidden` is a planner guard (not a prompt).
- **MRE operator-acknowledgement APIs.** `operatorMarkMissionTaskTriageState(taskID:state:)`, `acknowledgeTaskMissionEndRecovery(taskID:)`, and `acknowledgeTaskMissionEndAbort(taskID:)` already exist on `MissionRunEnvironment`. They are direct UI-driven calls today; Stage D prompts whose decision affects a task's triage state should call into these rather than mutate state in parallel.
- **Bottom-banner primitives.** `GuardianBottomPromptCenter` (`present(...)` single-dismiss, `presentChoice(...)` confirm/dismiss) and `GuardianBottomPromptBanner` are the existing solid-fill banner chrome used by MCR (recovery banner, abort banner, graceful-stop choice in `MissionControlSetupView`) and LiveDrive. Stage D reuses these as the rendering primitive for the new generic `GuardianPromptPanel` — the chrome stays, the management indirection becomes `OperatorPromptCenter`.
- **App-wide drawer primitive.** `AppDrawer` + `withAppDrawer()` and `SidebarOverlay` + `withSidebarOverlay()` are the trailing slide-in primitives on the window root. Stage D's top-bar in-app notifications inbox uses one of these (drawer for full inbox surface, overlay for transient peek); routing all cross-context prompts to a single drawer is what makes prompts addressable from any screen.
- **OOA notifications.** `UserNotificationService` is the macOS UNUserNotification adapter; `stubNotifyMissionRunOperatorPrompt(runID:missionName:summary:)` is the placeholder hook that Stage D's router replaces with the real `OperatorPromptEvent` delivery when the operator is out-of-app.

Open follow-up surfaced during the audit (not part of Stage D's scope):

- **Engagement disposition gating must land in MRE planner code, not in Stage D.** Stage D supplies the channel; the planner is the publisher. Until a planner consumes `.ask` / `.defer` / `.handoff`, those dispositions remain inert config and the prompt channel is exercised only by Layer 2 recipe escalations and migrated MCR/LD status banners.
- **Abort-policy / RTL-recipe circular-loop.** Once `MissionRunAbortPolicy.returnToLaunch` (and similar) route through a recipe path that itself contains a `confirmInLiveMission` matcher, an operator-initiated abort will re-prompt the very operator who initiated it. The publisher-side `OperatorDecisionCache.policyKey` mechanism is the right hook — the abort policy publisher seeds the cache for the about-to-run recipe so its confirmation matcher auto-resolves `.acknowledge`. The MRE policies / RoE system upgrade that exposes this is a downstream task tracked outside Stage D.

### Operator-prompt event type

Live at `Sources/GuardianHQ/Systems/OperatorPrompts/OperatorPromptEvent.swift`. The unified payload every Guardian process publishes through Stage D's `OperatorPromptCenter`; the routing / center / cache / UI primitives plug into this shape.

- **`OperatorPromptEvent`** — `id`, `origin`, **`displaySource`** (``OperatorPromptDisplaySource`` — operator-facing “who surfaced this”; default ``missionControl`` on `init(fromRecipeEscalation:)` when the caller omits ``displaySource``), `target`, `severity`, `title`, `body`, `contextFacts`, `options?`, `allowedVerbs`, `policyKey?`, `createdAt`, `expiresAt`. MC-R / Live Drive / Decisions / sticky toast show **Source: …** from `displaySource` via ``OperatorPromptAttributionCaption``; keep ``origin`` for routing only. Shape parallels ``FleetRecipeEscalationEvent`` so a recipe escalation lifts losslessly via `OperatorPromptEvent.init(fromRecipeEscalation:)`.
- **`OperatorPromptDisplaySource`** — `missionControl` | `mre` | `assistant(pluginID, displayName, operatorPromptBackgroundHex?)`. **MC-R mission-run** recipe escalations (``MissionRunRecipeOperatorPromptBridge/awaitMissionRecipeEscalationAnswer``) pass ``mre`` so non-assistant fleet prompts on the run strip match **Mission run** attribution alongside MRE engagement prompts; the recipe-escalation initializer still defaults ``missionControl`` for generic lifts (tests, future publishers). **Card fills:** ``OperatorPromptChrome/missionRunStackPromptCardBackgroundHex6`` (``#dbde9b``) applies to ``missionControl`` and ``mre`` prompts on MC-R / Live Drive / Decisions / persistent chips; **assistants** pass ``operatorPromptBackgroundHex`` (`#RRGGBB` or `RRGGBB`) from **plugin-owned data** only — Mission Control never hardcodes a plugin colour. **Fixed-reserve swap engagement:** ``MissionControlStore/raiseOperatorPromptSwapInReserve`` accepts ``operatorPromptDisplaySource`` (default ``mre``); headless callers pass ``assistant`` with their own hex when they want the pastel issuer fill (invalid / missing hex falls back to severity banner fills).
- **`OperatorPromptOrigin`** — closed v1 set: `recipeEscalation(event)`, `mreEngagementAsk(runID, action)`, `mreEngagementHandoff(runID, action)`, `freeform(source)`. New origin classes are additive; migrated status banners (MCR recovery / abort / graceful-stop, LiveDrive status) ride on `freeform` with a namespace-qualified source string until a tighter case is justified.
- **`OperatorPromptTarget` (addressing)** — first-class struct of optional fields the router, drawer, audit log, and `OperatorDecisionCache` all read uniformly: `missionRunID`, `missionTaskID`, `squad?`, `affectedRosterSlotID`, `affectedAssignmentID`, `affectedVehicleID`, `recipeRunID`, `pluginID`. Origin says **who is asking**; target says **what the prompt is about**. Defaults to `.unspecified` (every field nil → universal-drawer-only delivery, no panel match possible). Every publisher should fill in as much addressing as it knows so the operator can identify the prompt's scope before deciding.
- **`OperatorPromptSquadContext`** — squad addressing for a target. Squad is the set of slots attached to a single primary roster device (Guardian's roster model — `primary | wingman | reserve` with `leaderRosterDeviceId == primary.id`). Carries `primaryRosterDeviceID`, optional `primaryAssignmentID` + `primaryVehicleID`, and the `wingmanRosterDeviceIDs` + `reserveRosterDeviceIDs` lists so the renderer can show the full squad composition.
- **Target filter matching** — `OperatorPromptTarget.matches(_:)` is directional: receiver is the filter (panel's declared context), argument is the prompt's target. Nil filter fields are wildcards; non-nil filter fields must equal the prompt's corresponding field. Squad match is keyed on `primaryRosterDeviceID` because that's the squad's canonical identity.
- **`OperatorPromptContextFact` (rich context)** — ordered list of `(label, value)` operator-readable facts the publisher attaches so the operator has the data they need to decide. Each fact carries an `emphasis` (`normal | caption | success | warning | error`), optional SF Symbol `icon`, and optional `group` string for visual sectioning ("Where", "State", "Impact"). `Codable` so the audit log can persist them verbatim. Auto-populated facts (recipe-escalation lift adds `Recipe` + `Step` facts in group `"Recipe"`) precede caller-supplied facts.
- **`OperatorPromptOption`** — publisher-supplied labelled choice. Each option carries a stable `id`, `humanLabel`, optional `summary` (one-line description of the consequence — surfaced under the button text), `role` (`confirm | neutral | cancel` — drives blue / default / red per the app-wide button-color rule), an underlying ``FleetRecipeResumptionVerb`` (the closed transport every resolution flows through), and an optional typed `payload`. Custom options layer **on top of** the closed verb set — every option still resolves to one of `acknowledge | retry | skip | abort`.
- **Default option synthesis** — when `options` is `nil`, `OperatorPromptOption.standardOptions(forAllowedVerbs:)` synthesises a standard button set with ordering `acknowledge → retry → skip → abort` and role mapping `acknowledge=.confirm`, `retry=.neutral`, `skip=.neutral`, `abort=.cancel`. Sentinel ids `verb.<rawValue>` so the publisher can distinguish synthesised from author-supplied options. Existing recipe escalation matchers consume this path unchanged.
- **`OperatorPromptAnswer`** — `promptID`, `selectedOptionID`, `verb`, `remember`, `resolution`, `answeredAt`. The recipe runner reads only `verb`; the publisher reads `selectedOptionID` for branching; the prompt log (and the eventual RoE learner) consumes the whole record.
- **`OperatorPromptResolutionSource`** — closed v1: `operatorChose | rememberedFromCache | timeoutAborted`. Audit and learner code branches on this so a timeout-aborted decision is never weighted as training data.
- **Remember-this-choice gating** — `policyKey` non-nil ⇒ UI shows the "remember this choice" checkbox; nil ⇒ checkbox hidden (no cache to record into). Publisher consults `OperatorDecisionCache` (Stage D follow-up) at publish boundary; cache scope is the current run / session.
- **Timeout default** — five minutes (`OperatorPromptEvent.defaultTimeout`). `expiresAt = createdAt + timeout`; on expiry the router synthesises an answer with `resolution = .timeoutAborted`, `selectedOptionID = OperatorPromptOption.timeoutOptionID`, `verb = .abort` if `.abort` is allowed else the first ``OperatorPromptEvent/allowedVerbs`` entry.
- **Recipe-escalation default severities** — `operatorActionRequired → .warning`, `unrecoverableFailure → .error`, `confirmation → .info`. Construction overrides accept explicit `title` / `body` / `severity` / `options` / `policyKey` / `target` / `contextFacts` when the default phrasing or addressing is wrong for the context. The recipe runner doesn't know about missions, so the auto-populated target only fills `recipeRunID` + `affectedVehicleID`; publishers that *do* know the recipe is running inside a mission task (Stage E wizard, MRE) supply the rest by passing an explicit `target:` override.

### Operator-prompt delivery targets

Closed catalogue of the surfaces `OperatorPromptRouter` is allowed to dispatch to. Lives at `Sources/GuardianHQ/Systems/OperatorPrompts/OperatorPromptDeliveryTarget.swift`. One enum case per physical delivery channel; each case carries the addressing the channel needs to find its actual UI host (mission run id for the MCR panel, vehicle id + optional recipe-run id for the Vehicle Inspector wizard, etc.).

- **`mcrPromptPanel(missionRunID)`** — contextual MCR panel; matches only when the operator's MCR window is showing the same run.
- **`liveDrivePromptPanel(missionRunID?, vehicleID?)`** — contextual LiveDrive HUD panel. Setting only one field matches on that field; setting both tightens the match to require both; setting neither matches nothing (catalogue refuses fully-unaddressed LiveDrive targets to avoid silent broadcasts).
- **`vehicleInspectorWizardPanel(vehicleID, recipeRunID?)`** — contextual Vehicle Inspector wizard panel. Always requires the vehicle id to match; setting `recipeRunID` further restricts the channel to a single recipe run so unrelated escalations don't leak into a wizard's chrome.
- **`persistentToast`** — sticky corner toast (distinct from `ToastCenter`'s ephemeral toasts). Doesn't auto-dismiss; click opens the prompt in the inbox.
- **`userNotification(style:)`** — macOS `UNUserNotificationCenter` delivery. Style is `.banner` (standard list/banner/sound) or `.mcrCriticalReturn` (time-sensitive alert that pulls operator focus back into MCR on tap).
- **`inAppInbox`** — universal `AppDrawer`-hosted notifications inbox. Every prompt mirrors here regardless of contextual delivery so prompts that resolved or fired while the operator was looking elsewhere are still reviewable.

Role flags on every case so the router can build policies without case-matching everywhere:

- **`isContextual`** — `true` for the three panels; `false` for toast / OOA notification / inbox.
- **`isBroadcast`** — exact inverse of `isContextual`.
- **`isOutOfApp`** — `true` only for `.userNotification`; routing keeps OOA targets last in fallback policies and gates them on operator-presence heuristics so the app never double-notifies when the operator is on a contextual panel.
- **`isUniversalArchive`** — `true` only for `.inAppInbox`; the router reserves this slot as a mirror and never treats it as a primary target.

**Addressing match.** `OperatorPromptDeliveryTarget.accepts(eventTarget:)` is the single predicate every router pre-filter funnels through:

- Contextual targets reject any event whose `OperatorPromptTarget` doesn't satisfy their required id fields.
- LiveDrive matches when **either** the run id **or** the vehicle id (or both, if both are set on the target) match the event's target.
- Vehicle Inspector with `recipeRunID == nil` accepts any recipe run for the vehicle; with a non-nil `recipeRunID` it requires an exact match.
- Broadcast targets (toast / OOA notification / inbox) accept every event.

**`Kind` discriminator.** Every case has a stable rawValue (`mcrPromptPanel`, `liveDrivePromptPanel`, `vehicleInspectorWizardPanel`, `persistentToast`, `userNotification`, `inAppInbox`) used by the audit log, telemetry, and any serialised routing rules. These rawValues are locked — changing them requires a migration plan.

**Router contract (preview).** `ProcessPromptPolicy` (next Stage D item) resolves an event to an ordered list of targets. The router pre-filters with `accepts(eventTarget:)`, checks runtime availability (panel mounted, OOA permission granted, operator presence), picks the first available target as the **primary**, and mirrors to the rest. Plugins do **not** add new targets — they publish prompts that flow through the existing channels; the catalogue stays finite so audit-log shape is stable.

### Operator-prompt process policies

`ProcessPromptPolicy` (`Sources/GuardianHQ/Systems/OperatorPrompts/ProcessPromptPolicy.swift`) is the ordered fallback list a publisher's prompts fan through. The policy describes which channels a process **wants**; runtime availability and operator-presence filtering land in the router itself.

- **`entries: [Entry]`** — ordered channel templates: `mcrPanel`, `liveDrivePanel`, `vehicleInspectorWizard`, `persistentToast`, `userNotification(style:)`. Templates carry **no addressing** — they bind to a concrete `OperatorPromptDeliveryTarget` at resolve time using the event's `OperatorPromptTarget`.
- **`mirrorToInbox: Bool`** (default `true`) — when set, `inAppInbox` is appended last so every prompt is reviewable from the universal drawer. The inbox is **not** an `Entry` case; representing it as a flag keeps entry lists focused on operator-facing channels and makes "every prompt is reviewable" a uniform guarantee instead of a per-policy concern.
- **`resolveTargets(for:)`** — pure resolution: walks `entries` in order, binds each to a concrete target using `event.target`, skips entries whose required addressing is absent (e.g. `mcrPanel` skipped when the event has no `missionRunID`), and appends the inbox when `mirrorToInbox`. The router applies runtime availability filtering on top of this list.

Binding rules:

| Entry | Required event addressing | Concrete target |
|---|---|---|
| `mcrPanel` | `missionRunID` | `.mcrPromptPanel(missionRunID:)` |
| `liveDrivePanel` | `missionRunID` **or** `affectedVehicleID` (at least one) | `.liveDrivePromptPanel(missionRunID:, vehicleID:)` |
| `vehicleInspectorWizard` | `affectedVehicleID` (required); `recipeRunID` (forwarded if present) | `.vehicleInspectorWizardPanel(vehicleID:, recipeRunID:)` |
| `persistentToast` | — | `.persistentToast` |
| `userNotification(style:)` | — | `.userNotification(style:)` |

**Default policies per origin** (`ProcessPromptPolicy.default(for:)`):

- `recipeEscalation` — MCR → wizard → LiveDrive → toast → standard banner. MCR first when the prompt carries a `missionRunID` (MRE headless recipes); inspector-only runs still resolve the wizard when no run is addressed.
- `mreEngagementAsk` — MCR → LiveDrive → toast → `mcrCriticalReturn` notification. MRE asking permission for `rtl` / `land` / `forceDisarm` / `swapInReserve` needs operator attention back at MCR; the OOA variant pulls them back.
- `mreEngagementHandoff` — LiveDrive → MCR → `mcrCriticalReturn`. Handoff asks the operator to drive; LiveDrive is the takeover surface.
- `freeform` — MCR → LiveDrive → wizard → toast → standard banner. Broad coverage; specialised publishers should declare a tailored policy rather than ride the freeform default.

All defaults mirror to inbox. The router's policy provider (Stage D follow-up) can override these per process.

### Decisions drawer — **Review** (shell navigation)

When an inbox row can jump the operator to first-party chrome for context (not only answer in-place), the UI uses ``OperatorPromptReviewSurfaceResolver/resolve(for:)``. Resolution matches **routing policy order** via ``ProcessPromptPolicy/routingPolicy(for:)`` and the same ``ProcessPromptPolicy/bind(entry:to:)`` rules as ``OperatorPromptRouter``; it only **consumes** targets that map to sidebar sections (Mission Control run, Live Drive session). Inbox, sticky toast, OS notification, and Vehicle Inspector are skipped for this jump so the catalogue of shell destinations stays small.

- **Surfaces** — ``OperatorPromptReviewSurface``: ``missionControlRun(runID:missionTaskID?)``, ``liveDriveSession(vehicleID:missionRunID?)``, ``pluginSurface(applicationNamespace:parameters:)``.
- **App wiring** — ``OperatorPromptReviewFocusController`` (window ``environmentObject``) sets the pending sidebar section and drill-in ids; ``RootView``, ``MissionControlView``, and ``LiveDriveView`` consume those once.
- **Plugins / future systems** — After built-in resolution returns nil, ``OperatorPromptReviewSurfaceContributorRegistry`` runs registered contributors (idempotent per contributor id). A contributor returns ``pluginSurface``; the shell dismisses the drawer and posts ``Notification.Name.operatorPromptReviewPluginNavigation`` with payload keys on ``OperatorPromptReviewPluginNavigationUserInfoKey``. The plugin observes that notification and performs its own navigation (tabs, drill-in, modals).

### Operator-prompt router

`OperatorPromptRouter` (`Sources/GuardianHQ/Systems/OperatorPrompts/OperatorPromptRouter.swift`) is the pure decision component of Stage D. Lives on the main actor; consumed by `OperatorPromptCenter` (next item) for actual dispatch.

**Responsibilities (v1):**

1. Resolve the policy for an event's origin via `policyProvider`.
2. Resolve the policy's entries against the event's `OperatorPromptTarget` (delegates to `ProcessPromptPolicy.resolveTargets(for:)`).
3. Classify each resolved target via `availabilityProbe`:
   - Accepted → first one is `primary`, rest become `mirrors`.
   - Rejected → collected under `suppressed` for audit.
4. Return an `OperatorPromptRoutingDecision` value. The router does **not** dispatch; the center does.

**Why split router and center?** The decision is pure data and trivially testable; the center is stateful (host registry, in-flight prompts, timeouts, answer-fan-in). Keeping them separate means policy / routing edits don't touch any side-effect machinery, and exhaustive routing tests run with no UI fixture.

**`OperatorPromptRoutingDecision` shape:**

- `event` — the event being routed (kept on the decision so audit logging and downstream dispatch don't thread it separately).
- `primary: OperatorPromptDeliveryTarget?` — first available target in policy order. `nil` only when no target was available (reachable only when a policy with `mirrorToInbox = false` runs against a probe that rejects everything — the default policies always mirror to the always-available inbox).
- `mirrors: [OperatorPromptDeliveryTarget]` — available targets after the primary. Center dispatches to these too for cross-surface visibility; the first resolution wins and the center withdraws the rest.
- `suppressed: [OperatorPromptDeliveryTarget]` — targets the policy wanted but the probe rejected.
- `dispatched` (computed) — `[primary] + mirrors` when a primary exists, else just `mirrors`.
- `isUnroutable` (computed) — `primary == nil && mirrors.isEmpty`. Center uses this to escalate (e.g. force an OOA `mcrCriticalReturn`) or to mark the event as queued.

**Injection points:**

- `policyProvider: @MainActor (OperatorPromptOrigin) -> ProcessPromptPolicy` — defaults to `ProcessPromptPolicy.default(for:)`. Center can swap for per-process overrides.
- `availabilityProbe: @MainActor (OperatorPromptDeliveryTarget) -> Bool` — defaults to **inbox-only availability**. Center installs a real host-registry-backed probe at app start; the default keeps the router safe to construct before any host has registered (prompts route to the inbox and queue for the operator's next visit).

The inbox-only default is the v1 boot fallback: until the center wires up host discovery, the router still produces a well-formed decision that routes everything to the universal archive. Prompts don't get lost during startup.

### Operator-prompt resumption channel

`OperatorPromptResumptionChannel` (`Sources/GuardianHQ/Systems/OperatorPrompts/OperatorPromptResumptionChannel.swift`) is the answer-side transport. Carries operator picks back from delivery surfaces to the originating publisher (recipe runner escalation handler, MRE engagement planner, freeform plugin or migrated status banner). Pure transport — no host knowledge, no routing decisions, no dispatch.

**Publisher API.** A single async call:

```swift
let answer = await OperatorPromptResumptionChannel.shared.awaitAnswer(for: event)
```

`answer.verb` is the closed transport (``FleetRecipeResumptionVerb``) every process consumes; `answer.selectedOptionID` gives publisher-side branching; `answer.remember` feeds the future `OperatorDecisionCache`; `answer.resolution` distinguishes operator-chose vs cache-hit vs timeout-aborted.

**Host / center / cache API.**

- `submit(_:) -> Bool` — applies an answer to a pending event. Returns `true` when a waiter was resumed; `false` when no waiter was found (race or stray submission). Audit-stream emits on every successful application.
- `resolveExpiry(for:) -> Bool` — synthesises a timeout answer and submits it. Equivalent to `submit(event.synthesisedTimeoutAnswer())`.

**Cancellation.** `awaitAnswer(for:)` respects Swift Task cancellation. When the publisher's task is cancelled while awaiting an answer, the channel resolves the pending event with a synthesised timeout-style answer and cleans up the continuation. Cancellation propagates end-to-end without the publisher needing custom logic.

**Already-expired events.** `awaitAnswer(for:)` short-circuits when `event.isExpired()` is true at call time — returns the synthesised timeout answer immediately without ever installing a continuation. Keeps the publisher's `await` honest when an event slips through with a stale `expiresAt`.

**Audit stream.** `allAnswers` is a Combine publisher that emits every resolved answer in publish order. The inbox / audit-log surfaces subscribe to it; the channel itself does not retain history (the inbox owns persistence).

**Timeout-answer synthesis.** `OperatorPromptEvent.synthesisedTimeoutAnswer(at:)` is the canonical builder for timeout / cancellation answers. Prefers `.abort` when in `allowedVerbs`; otherwise the first allowed verb; final fallback is `.abort` (guard for the unsafe `allowedVerbs == []` construction). `selectedOptionID = OperatorPromptOption.timeoutOptionID` (`"verb.timeout"`); `remember = false`; `resolution = .timeoutAborted`.

**Why split channel and center?** Same rationale as splitting router and center: the channel is a thin pure transport with a tiny state surface (one dictionary of pending continuations) and is exhaustively testable on its own. The center wraps it with host registration, dispatch, expiry timer, and policy-provider injection.

### Extension points

- **New core command** → add to `FleetVehicleCoreCommandRegistrations.registerAll()` plus stack-converter coverage (or a documented `.notImplemented`).
- **New subsystem recipes** → add the recipe to its subsystem's `registerAll()` entry point (e.g. `FleetCalibrationRecipeRegistrations.registerAll()`). The bootstrap already invokes calibration and errors entry points; new subsystems get a sibling directory under `Subsystems/`, a JSON catalogue resource (declared in `Package.swift`), a `@MainActor` entry-point enum, and one extra `registerAll()` call inside `FleetRecipesCatalogueBootstrap.ensureRegistered()`.
- **Plugin contributions** → register through `GuardianPluginBootstrap`; each ``GuardianPluginManifest`` carries ``publishedCommandNamespaces`` / ``publishedRecipeNamespaces`` (under the plugin’s `command.<fleetNamespaceTail>` / `recipe.<fleetNamespaceTail>` roots) plus ``invokedCommandNamespaces`` / ``invokedRecipeNamespaces`` for every **non-owned** `command.*` / `recipe.*` the plugin’s recipes may call (e.g. `command.fleet.vehicle`, `recipe.fleet.calibrate`). Authoring guide: ``Sources/GuardianHQ/Plugins/PLUGIN_FLEET_CONTRIBUTIONS.md``. ``FleetCommandsCatalogue`` / ``FleetRecipesCatalogue`` enforce publish claims; ``FleetRecipeBodyParser`` and ``FleetRecipeRunner`` (and ``invoke(..., invokingPluginID:)``) enforce invoked claims. Keep manifest lists aligned with real registrations.

### Standing rule

Do **not** treat a registered descriptor as "done" unless the stack converter either performs a real, verifiable action **or** returns a deliberate, documented `.notImplemented` because the required transport is genuinely unavailable. The same rule applies to recipe bodies — a registered body that no step can actually run is worse than no body at all.

## Adding another autopilot “stack” later

Rough checklist so fleet badges, sim picker, and MAVLink stay consistent:

1. **`FleetAutopilotStack`** (`Sources/GuardianHQ/FleetAutopilotStack.swift`) — add a `case`, `displayName`, and `badgeBackground` for the new stack.
2. **`SimulationPlatform`** (`Sources/GuardianHQ/SimulationCatalog.swift`) — add a case if Guardian will **spawn** that stack from the app; wire the picker and `SitlService`.
3. **Live vehicle detection** — extend `mavsdk_bridge.py`: after `connect()`, map vendor/product (or MAV enums) to the same **lowercase** string you use as `FleetAutopilotStack.rawValue`, and emit `{"type":"vehicle_stack","stack":"<raw>"}`. Handle it in `FleetLinkService.handleBridgeStdoutLine` (already decodes `vehicle_stack`).
4. **SITL launch** — implement a recipe in `SitlLaunchRecipe.swift` / `SitlService.swift` (see ArduPilot and PX4 paths).

Keep `vehicle_stack` JSON values aligned with `FleetAutopilotStack` `rawValue` strings (`ardupilot`, `px4`, `unknown`, plus any new cases).

## Child processes and cold launch (orphan cleanup)

After a **force quit**, macOS can leave Guardian-spawned subprocesses running. On each **cold app launch**, `GuardianSitlOrphanBlitz` tears down known orphans: persisted root PIDs from `GuardianSitlSpawnRegistry`, ArduPilot/PX4-oriented `pgrep` patterns derived from `SitlLaunchRecipe`, and the **whole process tree** under each matched root (so helpers like MAVProxy or `px4-mavlink` go away with the parent).

**If you add new spawn methods or long-lived services**—including anything **outside** ArduPilot/PX4—you need to keep that behavior correct:

1. Prefer spawning through **`SitlProcessRunner`** (or the same registration hook it uses) so the root PID is recorded in **`GuardianSitlSpawnRegistry`** after `Process.run()` succeeds, and unregistered when the process exits cleanly.
2. If the new stack cannot use `SitlProcessRunner`, you must still **register** the root PID (same registry API) and ensure **`GuardianSitlOrphanBlitz`** can find it again after a crash—typically by adding **narrow, path-specific** `pgrep` patterns (see existing ArduPilot/PX4 patterns) so unrelated user processes are never matched.
3. For local debugging only, `GUARDIAN_SKIP_SITL_ORPHAN_BLITZ=1` disables the startup blitz.

Skipping the above leaves orphans after force quit and breaks the expectation that Guardian cleans up everything it started.
