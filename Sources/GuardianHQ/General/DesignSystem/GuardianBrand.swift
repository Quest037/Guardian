import SwiftUI

/// Guardian product brand tokens (in-app; SVG sources live under ``Resources/Brand``).
enum GuardianBrand {
    /// Primary brand purple (#6B3DDB) — pointed G mark, wordmark glyph, and ``Resources/Brand`` SVGs.
    static let purple = Color(red: 0.4196, green: 0.2392, blue: 0.8588)
}
