import Foundation

// MARK: - OperatorPromptChrome

/// Locked **Mission Control / mission-run** operator-prompt **card fill** (Stage D). Not plugin data.
enum OperatorPromptChrome {

    /// RGB hex **without** `#` — product default for ``OperatorPromptDisplaySource/missionControl`` and ``mre`` card backgrounds on MC-R / Live Drive strips.
    static let missionRunStackPromptCardBackgroundHex6 = "dbde9b"
}
