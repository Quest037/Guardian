# TODO

- **Built-in SITL:** ArduPilot tree under **Resources/ArduPilotSitl** (`make sitl-runtime`); Python **pexpect** via **`make sitl-deps`**; **MAVProxy** on PATH; `SitlLaunchRecipe` resolves **Bundle.module** first, optional **GUARDIAN_ARDUPILOT_ROOT** / **ARDUPILOT_ROOT** for dev overrides. Still to do: **PX4** in-app launcher; **non-conflicting MAVLink ports** per instance vs `mavsdk_server` extra connection URLs; CI caching for the ArduPilot clone; multi-vehicle mission rehearsal UX in Mission Control.
- **`mavsdk_server` lifecycle / orphans:** today teardown runs on **`NSApplication.willTerminate`** only (`FleetLinkService` → `stopServer()` → `Process.terminate()`); **force quit / crash** can leave a child listening on the gRPC / MAVLink ports. Add **attach-or-spawn** on **Server** start: probe **configured gRPC host:port** (and a small MAVSDK health/version check), reuse if compatible; otherwise spawn as today. Track whether we own the process so **Quit** only terminates what we started. Optional: bundle/rename binary (e.g. `guardian_mav_server`) for clearer `ps` / docs — **not** a substitute for gRPC probing.
- **SITL prewarm (future):** optional step (Makefile target, Xcode Run Script, or small CLI) that runs ArduPilot **waf** once to build `bin/arducopter` (and other common targets) under `Resources/ArduPilotSitl/build/sitl` *before* the user’s first **Add Sim** spawn, so the long one-time compile does not block the first in-app SITL session after a clean bundle / DerivedData.
- **Simulation catalog:** implement launch profiles for each `SimulationVehiclePreset` × `SimulationPlatform` (vehicle model, frame args, UDP topology) and surface in UI (add sim vehicle, optional per-spawn stack override vs General default).
- **Mission Control:** UI to add/remove simulated vehicles, bind to mission rehearsal (N vehicles for N-aircraft missions), integrate with the top-bar **Simulate** toggle and **FleetLinkService** / MAVLink settings.
- **Mission Control → Mission view:** include an optional **3D view** (vehicle pose / scene) alongside the existing mission UI when we get to that milestone.
- Make clicking non-input UI areas unfocus the currently focused input field (desktop blur-on-background-click behavior).
- Replace the **MAVSDK-Python sidecar** (`mavsdk_bridge.py` / `MavsdkBridgeRunner`) with a **native Swift gRPC client** generated from the same MAVSDK proto revision as the bundled `mavsdk_server`, once APIs and shipping story are stable (remove Python runtime dependency for end users).
